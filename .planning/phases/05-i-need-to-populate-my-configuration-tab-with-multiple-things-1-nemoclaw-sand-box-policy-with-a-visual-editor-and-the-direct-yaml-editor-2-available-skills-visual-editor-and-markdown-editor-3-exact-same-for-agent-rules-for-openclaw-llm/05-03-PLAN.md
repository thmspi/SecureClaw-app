---
phase: 05-i-need-to-populate-my-configuration-tab-with-multiple-things-1-nemoclaw-sand-box-policy-with-a-visual-editor-and-the-direct-yaml-editor-2-available-skills-visual-editor-and-markdown-editor-3-exact-same-for-agent-rules-for-openclaw-llm
plan: 03
type: tdd
wave: 2
depends_on: [05-01]
files_modified:
  - src/main/configuration/documents/openclaw-workspace-document.ts
  - src/main/configuration/documents/openclaw-workspace-document.test.ts
autonomous: true
requirements: [CFG-01, CFG-03, CFG-04]
must_haves:
  truths:
    - "OpenClaw skill markdown documents and agent-rules markdown document are discoverable through configuration IPC."
    - "Skill markdown supports visual metadata editing (frontmatter) and raw markdown editing with validation."
    - "Missing workspace/skill paths return explicit unavailable states and guidance instead of crashes."
  artifacts:
    - path: "src/main/configuration/documents/openclaw-workspace-document.ts"
      provides: "Markdown/frontmatter adapter for skills and AGENTS rules document."
    - path: "src/main/configuration/documents/openclaw-workspace-document.test.ts"
      provides: "Behavior tests for workspace path discovery, frontmatter parsing, save, and validation."
  key_links:
    - from: "src/main/configuration/documents/openclaw-workspace-document.ts"
      to: "src/main/configuration/configuration-service.ts"
      via: "adapter implementation imported by service skeleton"
      pattern: "createOpenClawWorkspaceAdapter"
---

<objective>
Implement OpenClaw workspace markdown adapters for skills and agent rules so the configuration tab can offer both visual and raw markdown editing modes.

Purpose: Deliver the second and third document families requested by the user (skills and agent/rules).
Output: Tested workspace adapter that discovers, loads, validates, and saves skill and AGENTS markdown docs.
</objective>

<execution_context>
@/Users/thomas/.codex/get-shit-done/workflows/execute-plan.md
@/Users/thomas/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-i-need-to-populate-my-configuration-tab-with-multiple-things-1-nemoclaw-sand-box-policy-with-a-visual-editor-and-the-direct-yaml-editor-2-available-skills-visual-editor-and-markdown-editor-3-exact-same-for-agent-rules-for-openclaw-llm/05-RESEARCH.md
@src/main/configuration/configuration-service.ts
@src/main/configuration/documents/openclaw-workspace-document.ts
@src/shared/configuration/configuration-contracts.ts

<interfaces>
From src/shared/configuration/configuration-contracts.ts (created in 05-01):
```typescript
type ConfigurationDocumentKind = 'nemoclaw-policy' | 'openclaw-skill' | 'openclaw-agent-rules';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing tests for workspace document discovery and markdown/frontmatter behavior</name>
  <read_first>
    src/main/configuration/documents/openclaw-workspace-document.ts
    src/main/configuration/documents/nemoclaw-policy-document.test.ts
    .planning/phases/05-i-need-to-populate-my-configuration-tab-with-multiple-things-1-nemoclaw-sand-box-policy-with-a-visual-editor-and-the-direct-yaml-editor-2-available-skills-visual-editor-and-markdown-editor-3-exact-same-for-agent-rules-for-openclaw-llm/05-RESEARCH.md
  </read_first>
  <files>src/main/configuration/documents/openclaw-workspace-document.test.ts</files>
  <behavior>
    - Test 1: listDocuments discovers skill docs from `~/.openclaw/skills/*/SKILL.md` and `<workspace>/skills/*/SKILL.md`.
    - Test 2: listDocuments always includes `openclaw-agent-rules:workspace` mapped to `<workspace>/AGENTS.md`.
    - Test 3: skill validation fails when frontmatter `name` or `description` is missing.
    - Test 4: missing workspace path returns a non-crashing unavailable summary with explicit guidance text.
  </behavior>
  <action>
    Create `src/main/configuration/documents/openclaw-workspace-document.test.ts` with mocked fs access.
    Use default workspace root `${homedir()}/.openclaw/workspace` when `OPENCLAW_WORKSPACE_DIR` is not set.
    Add assertions for document IDs:
    `openclaw-agent-rules:workspace`,
    `openclaw-skill:global:<skill-folder>`,
    `openclaw-skill:workspace:<skill-folder>`.
    Add validation expectations requiring skill frontmatter keys `name` and `description` as non-empty strings.
  </action>
  <verify>
    <automated>npm test -- src/main/configuration/documents/openclaw-workspace-document.test.ts --runInBand --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/main/configuration/documents/openclaw-workspace-document.test.ts` exits 0.
    - `rg -n "openclaw-agent-rules:workspace" src/main/configuration/documents/openclaw-workspace-document.test.ts` returns one match.
    - `rg -n "openclaw-skill:global" src/main/configuration/documents/openclaw-workspace-document.test.ts` returns one match.
    - `rg -n "name.*description" src/main/configuration/documents/openclaw-workspace-document.test.ts` returns at least one match in validation assertions.
  </acceptance_criteria>
  <done>Workspace adapter tests define expected behavior and fail against 05-01 stub.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement workspace markdown adapter for skills and agent rules</name>
  <read_first>
    src/main/configuration/documents/openclaw-workspace-document.ts
    src/main/configuration/documents/openclaw-workspace-document.test.ts
    src/main/configuration/configuration-service.ts
    src/shared/configuration/configuration-contracts.ts
  </read_first>
  <files>src/main/configuration/documents/openclaw-workspace-document.ts</files>
  <behavior>
    - Test 1: adapter loads skill docs with frontmatter/body decomposition and loads AGENTS.md as markdown document.
    - Test 2: adapter validate/save respects skill frontmatter constraints and persists markdown updates.
    - Test 3: adapter returns unavailable guidance when workspace path or doc file is missing.
  </behavior>
  <action>
    Replace stub implementation with `createOpenClawWorkspaceAdapter(deps?)`.
    Implement skill discovery in exactly two roots:
    `${homedir()}/.openclaw/skills/*/SKILL.md` and `${workspaceRoot}/skills/*/SKILL.md`,
    where `workspaceRoot` is `process.env.OPENCLAW_WORKSPACE_DIR ?? ${homedir()}/.openclaw/workspace`.
    Implement AGENTS document path `${workspaceRoot}/AGENTS.md` with fixed id `openclaw-agent-rules:workspace`.
    Parse markdown with `gray-matter`; return payload containing:
    `rawText` (full markdown),
    `visualModel.frontmatter`,
    `visualModel.sections` (split by `## ` headings),
    and `format: 'markdown'`.
    Validate skill docs with required frontmatter keys: `name` and `description`; AGENTS document requires non-empty markdown body.
    Save skill docs using `gray-matter` serialization and save AGENTS doc as plain markdown.
    For missing workspace or missing document path, return `errorCode: 'CONFIG_PATH_UNAVAILABLE'` and `userMessage` containing the missing absolute path.
  </action>
  <verify>
    <automated>npm test -- src/main/configuration/documents/openclaw-workspace-document.test.ts --runInBand --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "createOpenClawWorkspaceAdapter" src/main/configuration/documents/openclaw-workspace-document.ts` returns one match.
    - `rg -n "gray-matter" src/main/configuration/documents/openclaw-workspace-document.ts` returns at least one import/usage match.
    - `rg -n "openclaw-agent-rules:workspace" src/main/configuration/documents/openclaw-workspace-document.ts` returns one match.
    - `rg -n "CONFIG_PATH_UNAVAILABLE" src/main/configuration/documents/openclaw-workspace-document.ts` returns at least one match.
  </acceptance_criteria>
  <done>OpenClaw workspace adapter supports skills + agent-rules markdown workflows with validation and explicit path availability handling.</done>
</task>

</tasks>

<verification>
Run `npm test -- src/main/configuration/documents/openclaw-workspace-document.test.ts --runInBand --passWithNoTests`.
</verification>

<success_criteria>
- Skill and agent-rules docs are discoverable through typed adapter operations.
- Markdown docs support dual representation (raw + visual frontmatter/sections).
- Missing workspace and missing files produce deterministic guidance envelopes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-i-need-to-populate-my-configuration-tab-with-multiple-things-1-nemoclaw-sand-box-policy-with-a-visual-editor-and-the-direct-yaml-editor-2-available-skills-visual-editor-and-markdown-editor-3-exact-same-for-agent-rules-for-openclaw-llm/05-03-SUMMARY.md`
</output>
