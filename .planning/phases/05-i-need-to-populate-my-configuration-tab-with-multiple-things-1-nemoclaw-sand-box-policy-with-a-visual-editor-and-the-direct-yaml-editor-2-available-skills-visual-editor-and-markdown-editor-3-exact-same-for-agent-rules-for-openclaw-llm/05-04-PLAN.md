---
phase: 05-i-need-to-populate-my-configuration-tab-with-multiple-things-1-nemoclaw-sand-box-policy-with-a-visual-editor-and-the-direct-yaml-editor-2-available-skills-visual-editor-and-markdown-editor-3-exact-same-for-agent-rules-for-openclaw-llm
plan: 04
type: execute
wave: 3
depends_on: [05-02, 05-03]
files_modified:
  - src/renderer/stores/configuration-store.ts
  - src/renderer/pages/management/ConfigurationPanel.tsx
  - src/renderer/pages/management/ManagementPage.tsx
  - src/preload/platform-api.d.ts
autonomous: true
requirements: [CFG-01, CFG-02, CFG-04]
must_haves:
  truths:
    - "Configuration tab shows three functional configuration surfaces: NemoClaw policy, OpenClaw skills, and OpenClaw agent rules."
    - "Each document supports both visual mode and raw text mode, with explicit validation before save/apply."
    - "UI clearly shows unavailable apply/path states (for missing NemoClaw/OpenShell/workspace) without blocking editing."
  artifacts:
    - path: "src/renderer/stores/configuration-store.ts"
      provides: "Configuration tab state, mode switching, dirty tracking, and IPC action wrappers."
    - path: "src/renderer/pages/management/ConfigurationPanel.tsx"
      provides: "Document list, visual/raw editors, validation/save/apply controls, and availability messages."
    - path: "src/renderer/pages/management/ManagementPage.tsx"
      provides: "Configuration tab mounted to real configuration panel."
  key_links:
    - from: "src/renderer/stores/configuration-store.ts"
      to: "window.secureClaw.configuration"
      via: "list/load/validate/save/apply bridge methods"
      pattern: "secureClaw\\.configuration"
    - from: "src/renderer/pages/management/ManagementPage.tsx"
      to: "src/renderer/pages/management/ConfigurationPanel.tsx"
      via: "configuration tab render path"
      pattern: "ConfigurationPanel"
---

<objective>
Deliver the full Configuration tab UX with dual-mode editors and typed save/validate/apply flows for NemoClaw policy, OpenClaw skills, and agent rules.

Purpose: Complete the user-visible Phase 5 objective by replacing the configuration placeholder with usable editors.
Output: Working configuration store + configuration panel integrated into management navigation.
</objective>

<execution_context>
@/Users/thomas/.codex/get-shit-done/workflows/execute-plan.md
@/Users/thomas/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-i-need-to-populate-my-configuration-tab-with-multiple-things-1-nemoclaw-sand-box-policy-with-a-visual-editor-and-the-direct-yaml-editor-2-available-skills-visual-editor-and-markdown-editor-3-exact-same-for-agent-rules-for-openclaw-llm/05-RESEARCH.md
@src/renderer/pages/management/ManagementPage.tsx
@src/renderer/stores/management-store.ts
@src/preload/platform-api.ts
@src/shared/configuration/configuration-contracts.ts

<interfaces>
From src/renderer/pages/management/ManagementPage.tsx:
```typescript
type ManagementTab = 'management' | 'configuration' | 'settings';
```

From src/preload/platform-api.ts (created in 05-01):
```typescript
window.secureClaw.configuration.listDocuments()
window.secureClaw.configuration.loadDocument(...)
window.secureClaw.configuration.validateDocument(...)
window.secureClaw.configuration.saveDocument(...)
window.secureClaw.configuration.applyDocument(...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create configuration store with dual-mode editor state and typed IPC actions</name>
  <read_first>
    src/renderer/stores/management-store.ts
    src/preload/platform-api.ts
    src/preload/platform-api.d.ts
    src/shared/configuration/configuration-contracts.ts
  </read_first>
  <files>src/renderer/stores/configuration-store.ts, src/preload/platform-api.d.ts</files>
  <action>
    Create `src/renderer/stores/configuration-store.ts` with zustand state fields:
    `documents`, `activeDocumentId`, `activeDocument`, `editorMode`, `rawText`, `visualModel`,
    `dirty`, `loading`, `validating`, `saving`, `applying`, `validationIssues`, `lastError`, `lastApplyResult`.
    Implement actions:
    `loadDocuments()`, `openDocument(documentId)`, `setEditorMode(mode)`, `setRawText(value)`,
    `setVisualModel(value)`, `validateActiveDocument()`, `saveActiveDocument()`, `applyActiveDocument(applyMode)`, `resetDraft()`.
    All actions must call the preload bridge `window.secureClaw.configuration.*` methods created in 05-01 and keep store state non-persistent.
    Extend `src/preload/platform-api.d.ts` so renderer typing includes the new `configuration` API methods and request/response shapes.
  </action>
  <verify>
    <automated>npm run build:renderer</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/renderer/stores/configuration-store.ts` exits 0.
    - `rg -n "secureClaw\\.configuration\\.listDocuments" src/renderer/stores/configuration-store.ts` returns one match.
    - `rg -n "setEditorMode" src/renderer/stores/configuration-store.ts` returns one match.
    - `rg -n "configuration" src/preload/platform-api.d.ts` returns at least one match.
  </acceptance_criteria>
  <done>Renderer has a dedicated configuration store for list/load/edit/validate/save/apply workflows.</done>
</task>

<task type="auto">
  <name>Task 2: Build ConfigurationPanel with visual editor and raw editor per document kind</name>
  <read_first>
    src/renderer/pages/management/ManagementPage.tsx
    src/renderer/pages/management/SettingsPanel.tsx
    src/renderer/stores/configuration-store.ts
    src/shared/configuration/configuration-contracts.ts
    .planning/phases/05-i-need-to-populate-my-configuration-tab-with-multiple-things-1-nemoclaw-sand-box-policy-with-a-visual-editor-and-the-direct-yaml-editor-2-available-skills-visual-editor-and-markdown-editor-3-exact-same-for-agent-rules-for-openclaw-llm/05-RESEARCH.md
  </read_first>
  <files>src/renderer/pages/management/ConfigurationPanel.tsx</files>
  <action>
    Create `ConfigurationPanel.tsx` with these concrete UI sections:
    left document list grouped as:
    `NemoClaw Policy`,
    `Available Skills`,
    `Agent Rules`.
    top action row with:
    `Visual` / `Raw` mode toggle,
    `Validate`,
    `Save`,
    `Apply Static`,
    `Apply Dynamic` (show only for `nemoclaw-policy`).
    raw mode uses `@monaco-editor/react` with language:
    `yaml` for `nemoclaw-policy`,
    `markdown` for `openclaw-skill` and `openclaw-agent-rules`.
    visual mode:
    for `nemoclaw-policy`: render RJSF form (`@rjsf/core` + `@rjsf/validator-ajv8`) against a JSON schema covering `network_policies`, `endpoints`, `binaries`, `rules`.
    for markdown docs: render form fields for `frontmatter.name`, `frontmatter.description`, and a textarea for each `sections[]` entry.
    Render explicit unavailable warnings:
    `Install NemoClaw CLI to apply static sandbox policy.`
    `Install OpenShell CLI to apply dynamic sandbox policy.`
    `OpenClaw workspace path is unavailable. Set OPENCLAW_WORKSPACE_DIR or create ~/.openclaw/workspace.`
    Show validation issues in a deterministic list under the editor pane.
  </action>
  <verify>
    <automated>npm run build:renderer</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/renderer/pages/management/ConfigurationPanel.tsx` exits 0.
    - `rg -n "Apply Static" src/renderer/pages/management/ConfigurationPanel.tsx` returns one match.
    - `rg -n "Apply Dynamic" src/renderer/pages/management/ConfigurationPanel.tsx` returns one match.
    - `rg -n "@monaco-editor/react" src/renderer/pages/management/ConfigurationPanel.tsx` returns one import match.
    - `rg -n "@rjsf/core" src/renderer/pages/management/ConfigurationPanel.tsx` returns one import match.
    - `rg -n "OpenClaw workspace path is unavailable" src/renderer/pages/management/ConfigurationPanel.tsx` returns one match.
  </acceptance_criteria>
  <done>Configuration panel renders all required document categories with dual-mode editing and explicit apply/unavailable UX.</done>
</task>

<task type="auto">
  <name>Task 3: Wire configuration tab to ConfigurationPanel and remove placeholder copy</name>
  <read_first>
    src/renderer/pages/management/ManagementPage.tsx
    src/renderer/pages/management/ConfigurationPanel.tsx
  </read_first>
  <files>src/renderer/pages/management/ManagementPage.tsx</files>
  <action>
    Update `ManagementPage.tsx` so the `configuration` tab renders `<ConfigurationPanel />` instead of the placeholder card text.
    Keep existing navigation ids (`management`, `configuration`, `settings`) unchanged.
    Ensure entering configuration tab triggers `loadDocuments()` from configuration store once per mount via `useEffect`.
  </action>
  <verify>
    <automated>npm run build:renderer</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "ConfigurationPanel" src/renderer/pages/management/ManagementPage.tsx` returns at least one import and one usage match.
    - `rg -n "No additional configuration is available yet" src/renderer/pages/management/ManagementPage.tsx` returns no matches.
    - `rg -n "loadDocuments" src/renderer/pages/management/ManagementPage.tsx` returns at least one match.
  </acceptance_criteria>
  <done>Configuration tab is connected to real configuration workflows and no longer shows placeholder content.</done>
</task>

</tasks>

<verification>
Run `npm run build:renderer`.
</verification>

<success_criteria>
- User can open Configuration tab and see NemoClaw policy, skills, and agent-rules documents.
- User can switch between visual and raw mode for each document kind.
- Validate, save, and apply actions are available with clear unavailable-state guidance when dependencies are missing.
</success_criteria>

<output>
After completion, create `.planning/phases/05-i-need-to-populate-my-configuration-tab-with-multiple-things-1-nemoclaw-sand-box-policy-with-a-visual-editor-and-the-direct-yaml-editor-2-available-skills-visual-editor-and-markdown-editor-3-exact-same-for-agent-rules-for-openclaw-llm/05-04-SUMMARY.md`
</output>
