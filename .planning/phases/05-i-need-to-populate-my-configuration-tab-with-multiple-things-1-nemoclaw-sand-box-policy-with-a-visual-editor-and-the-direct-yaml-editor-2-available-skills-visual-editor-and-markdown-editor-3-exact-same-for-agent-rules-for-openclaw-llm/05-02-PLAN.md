---
phase: 05-i-need-to-populate-my-configuration-tab-with-multiple-things-1-nemoclaw-sand-box-policy-with-a-visual-editor-and-the-direct-yaml-editor-2-available-skills-visual-editor-and-markdown-editor-3-exact-same-for-agent-rules-for-openclaw-llm
plan: 02
type: tdd
wave: 2
depends_on: [05-01]
files_modified:
  - src/main/configuration/documents/nemoclaw-policy-document.ts
  - src/main/configuration/documents/nemoclaw-policy-document.test.ts
autonomous: true
requirements: [CFG-03, CFG-04]
must_haves:
  truths:
    - "NemoClaw sandbox policy document can be loaded, validated, and saved as YAML through configuration IPC."
    - "Policy apply action explicitly distinguishes static apply (`nemoclaw onboard`) and dynamic apply (`openshell policy set ...`)."
    - "When required CLIs are unavailable, user receives actionable unavailability guidance instead of silent failure."
  artifacts:
    - path: "src/main/configuration/documents/nemoclaw-policy-document.ts"
      provides: "YAML adapter for NemoClaw policy with load/validate/save/apply logic."
    - path: "src/main/configuration/documents/nemoclaw-policy-document.test.ts"
      provides: "Behavioral test coverage for NemoClaw policy adapter paths."
  key_links:
    - from: "src/main/configuration/documents/nemoclaw-policy-document.ts"
      to: "src/main/configuration/configuration-service.ts"
      via: "adapter implementation imported by service skeleton"
      pattern: "createNemoClawPolicyAdapter"
---

<objective>
Implement the NemoClaw sandbox policy adapter behind the configuration IPC skeleton using YAML parsing and explicit static/dynamic apply semantics.

Purpose: Deliver the first concrete document family (`nemoclaw-policy`) with safe validation and command-availability handling.
Output: Fully working NemoClaw policy adapter and tests.
</objective>

<execution_context>
@/Users/thomas/.codex/get-shit-done/workflows/execute-plan.md
@/Users/thomas/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-i-need-to-populate-my-configuration-tab-with-multiple-things-1-nemoclaw-sand-box-policy-with-a-visual-editor-and-the-direct-yaml-editor-2-available-skills-visual-editor-and-markdown-editor-3-exact-same-for-agent-rules-for-openclaw-llm/05-RESEARCH.md
@src/main/configuration/configuration-service.ts
@src/main/configuration/documents/nemoclaw-policy-document.ts
@src/shared/configuration/configuration-contracts.ts

<interfaces>
From src/shared/configuration/configuration-contracts.ts (created in 05-01):
```typescript
type ConfigurationDocumentKind = 'nemoclaw-policy' | 'openclaw-skill' | 'openclaw-agent-rules';
```

From 05-01 service skeleton:
```typescript
createNemoClawPolicyAdapter()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing tests for NemoClaw policy load/validate/save/apply behavior</name>
  <read_first>
    src/main/configuration/documents/nemoclaw-policy-document.ts
    src/main/ipc/security-router.test.ts
    src/main/ipc/runtime-router.test.ts
    .planning/phases/05-i-need-to-populate-my-configuration-tab-with-multiple-things-1-nemoclaw-sand-box-policy-with-a-visual-editor-and-the-direct-yaml-editor-2-available-skills-visual-editor-and-markdown-editor-3-exact-same-for-agent-rules-for-openclaw-llm/05-RESEARCH.md
  </read_first>
  <files>src/main/configuration/documents/nemoclaw-policy-document.test.ts</files>
  <behavior>
    - Test 1: default document path resolves to `~/.openclaw/openclaw-sandbox.yaml` when no override is provided.
    - Test 2: YAML parse errors return `valid: false` with line/column issue details.
    - Test 3: static apply calls `nemoclaw onboard`; dynamic apply calls `openshell policy set <policy-path>`.
    - Test 4: missing `nemoclaw`/`openshell` returns `CONFIG_COMMAND_UNAVAILABLE` guidance envelope.
  </behavior>
  <action>
    Create `src/main/configuration/documents/nemoclaw-policy-document.test.ts` with mocked filesystem and command-runner dependencies.
    Add explicit cases for:
    `applyMode: 'static'` -> command array `['nemoclaw', 'onboard']`,
    `applyMode: 'dynamic'` -> command array `['openshell', 'policy', 'set', '<resolved-policy-path>']`.
    Assert unavailable command paths return:
    `errorCode: 'CONFIG_COMMAND_UNAVAILABLE'`,
    `userMessage` containing the missing command name,
    and `nextSteps[0]` containing `Install NemoClaw/OpenShell CLI`.
  </action>
  <verify>
    <automated>npm test -- src/main/configuration/documents/nemoclaw-policy-document.test.ts --runInBand --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/main/configuration/documents/nemoclaw-policy-document.test.ts` exits 0.
    - `rg -n "nemoclaw onboard" src/main/configuration/documents/nemoclaw-policy-document.test.ts` returns one match.
    - `rg -n "openshell', 'policy', 'set'" src/main/configuration/documents/nemoclaw-policy-document.test.ts` returns one match.
    - `rg -n "CONFIG_COMMAND_UNAVAILABLE" src/main/configuration/documents/nemoclaw-policy-document.test.ts` returns at least one match.
  </acceptance_criteria>
  <done>Test suite captures expected NemoClaw policy behavior and fails against the 05-01 stub.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement NemoClaw YAML adapter with static/dynamic apply paths</name>
  <read_first>
    src/main/configuration/documents/nemoclaw-policy-document.ts
    src/main/configuration/documents/nemoclaw-policy-document.test.ts
    src/main/configuration/configuration-service.ts
    src/shared/configuration/configuration-contracts.ts
  </read_first>
  <files>src/main/configuration/documents/nemoclaw-policy-document.ts</files>
  <behavior>
    - Test 1: adapter reads and writes YAML policy text with deterministic newline-ending output.
    - Test 2: validate returns structured issues when YAML parse fails.
    - Test 3: apply returns success for static/dynamic modes when commands succeed, and actionable envelope when unavailable.
  </behavior>
  <action>
    Replace stub implementation with `createNemoClawPolicyAdapter(deps?)` that exports concrete methods:
    `listDocuments`, `loadDocument`, `validateDocument`, `saveDocument`, `applyDocument`.
    Resolve baseline file path to `${homedir()}/.openclaw/openclaw-sandbox.yaml` unless a request `pathOverride` is supplied.
    Parse and stringify policy with `yaml` package; preserve UTF-8 text and end saved files with a trailing newline.
    Implement `validateDocument` to return `valid: false` and issue entries that include parser error message and location (line/column) when parse fails.
    Implement apply mode handling:
    `static` executes `nemoclaw onboard`,
    `dynamic` executes `openshell policy set <policyPath>`.
    Return `errorCode: 'CONFIG_COMMAND_UNAVAILABLE'` with concrete next steps when required command binary is missing.
  </action>
  <verify>
    <automated>npm test -- src/main/configuration/documents/nemoclaw-policy-document.test.ts --runInBand --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "createNemoClawPolicyAdapter" src/main/configuration/documents/nemoclaw-policy-document.ts` returns one match.
    - `rg -n "nemoclaw', 'onboard'" src/main/configuration/documents/nemoclaw-policy-document.ts` returns one match.
    - `rg -n "openshell', 'policy', 'set'" src/main/configuration/documents/nemoclaw-policy-document.ts` returns one match.
    - `rg -n "CONFIG_COMMAND_UNAVAILABLE" src/main/configuration/documents/nemoclaw-policy-document.ts` returns at least one match.
  </acceptance_criteria>
  <done>NemoClaw policy adapter is fully implemented and test-verified for load/validate/save/apply and command-unavailable fallbacks.</done>
</task>

</tasks>

<verification>
Run `npm test -- src/main/configuration/documents/nemoclaw-policy-document.test.ts --runInBand --passWithNoTests`.
</verification>

<success_criteria>
- NemoClaw policy document is editable as YAML through typed adapter operations.
- Static and dynamic apply paths are explicit and correctly command-bound.
- Command unavailability is surfaced with explicit, user-facing remediation.
</success_criteria>

<output>
After completion, create `.planning/phases/05-i-need-to-populate-my-configuration-tab-with-multiple-things-1-nemoclaw-sand-box-policy-with-a-visual-editor-and-the-direct-yaml-editor-2-available-skills-visual-editor-and-markdown-editor-3-exact-same-for-agent-rules-for-openclaw-llm/05-02-SUMMARY.md`
</output>
