---
phase: 05-i-need-to-populate-my-configuration-tab-with-multiple-things-1-nemoclaw-sand-box-policy-with-a-visual-editor-and-the-direct-yaml-editor-2-available-skills-visual-editor-and-markdown-editor-3-exact-same-for-agent-rules-for-openclaw-llm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/shared/configuration/configuration-contracts.ts
  - src/shared/ipc/configuration-channels.ts
  - src/shared/ipc/index.ts
  - src/main/configuration/configuration-service.ts
  - src/main/configuration/documents/nemoclaw-policy-document.ts
  - src/main/configuration/documents/openclaw-workspace-document.ts
  - src/main/ipc/configuration-router.ts
  - src/main/ipc/configuration-router.test.ts
  - src/main/index.ts
  - src/preload/platform-api.ts
  - src/preload/__tests__/platform-api.test.ts
autonomous: true
requirements: [CFG-03]
must_haves:
  truths:
    - "Renderer invokes configuration actions through a typed `window.secureClaw.configuration` bridge and not raw ipcRenderer."
    - "Main process validates all configuration IPC payloads before dispatching to domain services."
    - "Configuration domain exposes exactly five baseline operations: list, load, validate, save, apply."
  artifacts:
    - path: "src/shared/configuration/configuration-contracts.ts"
      provides: "Configuration document/request/response contracts shared across main, preload, and renderer."
    - path: "src/shared/ipc/configuration-channels.ts"
      provides: "Versioned configuration IPC channel constants and zod schemas."
    - path: "src/main/ipc/configuration-router.ts"
      provides: "Typed IPC handlers for configuration operations."
    - path: "src/preload/platform-api.ts"
      provides: "Narrow preload `configuration` bridge methods."
  key_links:
    - from: "src/preload/platform-api.ts"
      to: "src/main/ipc/configuration-router.ts"
      via: "configuration:v1:* ipcRenderer.invoke calls"
      pattern: "configuration:v1:"
    - from: "src/main/index.ts"
      to: "src/main/ipc/configuration-router.ts"
      via: "handler registration during startup"
      pattern: "registerConfigurationHandlers"
---

<objective>
Create the configuration foundation: dependencies, shared contracts, IPC schemas, router wiring, and preload bridge required for Phase 5 editors.

Purpose: Establish a typed configuration boundary first so downstream NemoClaw and OpenClaw document adapters can implement against stable interfaces.
Output: Buildable configuration domain scaffolding with validated channels and test coverage for router/preload exposure.
</objective>

<execution_context>
@/Users/thomas/.codex/get-shit-done/workflows/execute-plan.md
@/Users/thomas/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-i-need-to-populate-my-configuration-tab-with-multiple-things-1-nemoclaw-sand-box-policy-with-a-visual-editor-and-the-direct-yaml-editor-2-available-skills-visual-editor-and-markdown-editor-3-exact-same-for-agent-rules-for-openclaw-llm/05-RESEARCH.md
@src/shared/ipc/runtime-channels.ts
@src/main/ipc/runtime-router.ts
@src/main/index.ts
@src/preload/platform-api.ts

<interfaces>
From src/main/index.ts:
```typescript
registerRuntimeHandlers(ipcMain);
diagnosticsRouter.registerDiagnosticsHandlers(ipcMain);
securityRouter.registerSecurityHandlers(ipcMain);
```

From src/preload/platform-api.ts:
```typescript
contextBridge.exposeInMainWorld('secureClaw', {
  platform: platformAPI,
  install: installAPI,
  runtime: runtimeAPI,
  diagnostics: diagnosticsAPI,
  secrets: secretsAPI,
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add configuration dependencies and shared contracts/channels</name>
  <read_first>
    package.json
    src/shared/ipc/runtime-channels.ts
    src/shared/ipc/security-channels.ts
    src/shared/ipc/index.ts
  </read_first>
  <files>package.json, package-lock.json, src/shared/configuration/configuration-contracts.ts, src/shared/ipc/configuration-channels.ts, src/shared/ipc/index.ts</files>
  <action>
    Install these exact packages: `@monaco-editor/react@4.7.0`, `monaco-editor@0.55.1`, `monaco-yaml@5.4.1`, `yaml@2.8.3`, `@rjsf/core@6.4.2`, `@rjsf/validator-ajv8@6.4.2`, `gray-matter@4.0.3`, `json5@2.2.3`.
    Create `src/shared/configuration/configuration-contracts.ts` with concrete types for:
    `ConfigurationDocumentKind = 'nemoclaw-policy' | 'openclaw-skill' | 'openclaw-agent-rules'`,
    `ConfigurationDocumentFormat = 'yaml' | 'markdown'`,
    `ConfigurationEditorMode = 'visual' | 'raw'`,
    `ConfigurationDocumentSummary`, `ConfigurationDocumentPayload`, `ConfigurationValidationIssue`,
    and request/response interfaces for `listDocuments`, `loadDocument`, `validateDocument`, `saveDocument`, `applyDocument`.
    Create `src/shared/ipc/configuration-channels.ts` with versioned channels:
    `configuration:v1:listDocuments`,
    `configuration:v1:loadDocument`,
    `configuration:v1:validateDocument`,
    `configuration:v1:saveDocument`,
    `configuration:v1:applyDocument`,
    plus zod schemas for each request payload.
    Export `configuration-channels` from `src/shared/ipc/index.ts`.
  </action>
  <verify>
    <automated>npm run build:main</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "configuration:v1:listDocuments" src/shared/ipc/configuration-channels.ts` returns one match.
    - `rg -n "ConfigurationDocumentKind = 'nemoclaw-policy' \\| 'openclaw-skill' \\| 'openclaw-agent-rules'" src/shared/configuration/configuration-contracts.ts` returns one match.
    - `rg -n "export \\* from './configuration-channels';" src/shared/ipc/index.ts` returns one match.
    - `rg -n "\"@monaco-editor/react\"" package.json` returns one match.
    - `rg -n "\"gray-matter\"" package.json` returns one match.
  </acceptance_criteria>
  <done>Configuration dependencies and typed shared configuration contracts/channels exist and compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create configuration router/service skeleton and preload bridge wiring</name>
  <read_first>
    src/main/ipc/runtime-router.ts
    src/main/ipc/security-router.ts
    src/main/index.ts
    src/preload/platform-api.ts
    src/preload/__tests__/platform-api.test.ts
    src/shared/configuration/configuration-contracts.ts
    src/shared/ipc/configuration-channels.ts
  </read_first>
  <files>src/main/configuration/configuration-service.ts, src/main/configuration/documents/nemoclaw-policy-document.ts, src/main/configuration/documents/openclaw-workspace-document.ts, src/main/ipc/configuration-router.ts, src/main/ipc/configuration-router.test.ts, src/main/index.ts, src/preload/platform-api.ts, src/preload/__tests__/platform-api.test.ts</files>
  <behavior>
    - Test 1: `configuration-router` registers all five channels and zod-rejects invalid payloads.
    - Test 2: preload exposes `window.secureClaw.configuration.{listDocuments,loadDocument,validateDocument,saveDocument,applyDocument}`.
    - Test 3: no raw `ipcRenderer` object is added to renderer-facing configuration API.
  </behavior>
  <action>
    Create `src/main/configuration/configuration-service.ts` with a concrete `configurationService` object exposing:
    `listDocuments()`, `loadDocument(request)`, `validateDocument(request)`, `saveDocument(request)`, `applyDocument(request)`.
    Wire it to two adapters imported from:
    `src/main/configuration/documents/nemoclaw-policy-document.ts` and
    `src/main/configuration/documents/openclaw-workspace-document.ts`.
    In this plan, adapter files are stubs that return deterministic placeholder errors with:
    `errorCode: 'CONFIG_NOT_IMPLEMENTED'` and `userMessage: 'Configuration adapter not implemented yet.'`.
    Create `src/main/ipc/configuration-router.ts` to parse request payloads with zod schemas and dispatch to `configurationService`.
    Create `src/main/ipc/configuration-router.test.ts` mirroring existing runtime/security router test style.
    Register the new handlers in `src/main/index.ts` with `registerConfigurationHandlers(ipcMain)` in the same startup branch as other routers.
    Extend `src/preload/platform-api.ts` with a `configuration` object exposing:
    `listDocuments()`, `loadDocument(request)`, `validateDocument(request)`, `saveDocument(request)`, `applyDocument(request)`.
    Extend `src/preload/__tests__/platform-api.test.ts` to assert `configuration` methods exist and that raw `ipcRenderer` is still not exposed.
  </action>
  <verify>
    <automated>npm test -- src/main/ipc/configuration-router.test.ts src/preload/__tests__/platform-api.test.ts --runInBand --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "registerConfigurationHandlers\\(ipcMain\\)" src/main/index.ts` returns one match.
    - `rg -n "configuration:" src/preload/platform-api.ts` returns at least one match in `exposeInMainWorld`.
    - `rg -n "CONFIG_NOT_IMPLEMENTED" src/main/configuration/documents/nemoclaw-policy-document.ts src/main/configuration/documents/openclaw-workspace-document.ts` returns matches in both files.
    - `rg -n "configuration:v1:applyDocument" src/main/ipc/configuration-router.ts` returns one match.
    - `test -f src/main/ipc/configuration-router.test.ts` exits 0.
  </acceptance_criteria>
  <done>Configuration IPC/preload skeleton is wired and tested, with stable adapter interfaces for downstream plans.</done>
</task>

</tasks>

<verification>
Run `npm run build:main` and `npm test -- src/main/ipc/configuration-router.test.ts src/preload/__tests__/platform-api.test.ts --runInBand --passWithNoTests`.
</verification>

<success_criteria>
- Shared configuration contracts and versioned channels compile.
- Main process registers and serves `configuration:v1:*` handlers.
- Renderer can call typed configuration bridge methods through preload without direct privileged access.
</success_criteria>

<output>
After completion, create `.planning/phases/05-i-need-to-populate-my-configuration-tab-with-multiple-things-1-nemoclaw-sand-box-policy-with-a-visual-editor-and-the-direct-yaml-editor-2-available-skills-visual-editor-and-markdown-editor-3-exact-same-for-agent-rules-for-openclaw-llm/05-01-SUMMARY.md`
</output>
