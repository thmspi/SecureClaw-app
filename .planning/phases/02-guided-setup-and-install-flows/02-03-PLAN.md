---
phase: 02-guided-setup-and-install-flows
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/main/install/install-orchestrator.ts
  - src/main/install/rollback-service.ts
  - src/main/install/install-steps.ts
  - src/main/ipc/install-router.ts
  - src/preload/install-api.ts
  - src/preload/index.ts
  - src/renderer/hooks/use-install-progress.ts
  - src/renderer/hooks/use-prerequisites.ts
  - src/renderer/pages/wizard/PrerequisitesStep.tsx
  - src/renderer/pages/wizard/InstallStep.tsx
  - src/main/install/__tests__/install-orchestrator.test.ts
  - src/main/install/__tests__/rollback-service.test.ts
autonomous: true
requirements:
  - SETUP-01
  - SETUP-02
  - SETUP-03
  - SETUP-04
  - SETUP-05

must_haves:
  truths:
    - "User can trigger prerequisite checks from UI and see results (D-08)"
    - "User can start one-click install and see live progress streaming (SETUP-03, D-09)"
    - "User can cancel install and see confirmation dialog (D-14)"
    - "Rollback removes created artifacts on cancel or failure (D-15)"
    - "Retry resumes from failed step, skipping completed steps (D-13)"
    - "Install state persists in DB and recovers from crash (D-16)"
  artifacts:
    - path: "src/main/install/install-orchestrator.ts"
      provides: "Step-by-step install execution with progress events"
      exports: ["InstallOrchestrator"]
    - path: "src/main/install/rollback-service.ts"
      provides: "Artifact tracking and cleanup for rollback"
      exports: ["RollbackService"]
    - path: "src/main/ipc/install-router.ts"
      provides: "IPC handlers for install operations"
      exports: ["registerInstallHandlers"]
    - path: "src/preload/install-api.ts"
      provides: "Preload bridge for install IPC"
      exports: ["installAPI"]
    - path: "src/renderer/hooks/use-install-progress.ts"
      provides: "React hook for IPC event subscription"
      exports: ["useInstallProgress"]
  key_links:
    - from: "src/renderer/pages/wizard/InstallStep.tsx"
      to: "src/preload/install-api.ts"
      via: "window.secureClaw.install"
      pattern: "window\\.secureClaw\\.install"
    - from: "src/main/ipc/install-router.ts"
      to: "src/main/install/install-orchestrator.ts"
      via: "orchestrator dispatch"
      pattern: "InstallOrchestrator|orchestrator"
    - from: "src/main/install/install-orchestrator.ts"
      to: "src/main/install/install-state-service.ts"
      via: "state persistence"
      pattern: "saveInstallState|loadInstallState"
---

<objective>
Wire the complete install flow: IPC handlers, install orchestrator, rollback service, and renderer hooks for live progress streaming.

Purpose: Complete the end-to-end integration so users can run one-click installs with real-time progress, cancel with rollback, and retry from failure point (SETUP-03, SETUP-05).

Output: Fully functional install flow from UI trigger through main process execution with event streaming back to renderer.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-guided-setup-and-install-flows/02-CONTEXT.md
@.planning/phases/02-guided-setup-and-install-flows/02-RESEARCH.md
@.planning/phases/02-guided-setup-and-install-flows/02-01-SUMMARY.md
@.planning/phases/02-guided-setup-and-install-flows/02-02-SUMMARY.md

<interfaces>
<!-- From Plan 01: Install contracts and services -->
From src/shared/install/install-contracts.ts:
```typescript
export type PrerequisiteStatus = 'passed' | 'failed' | 'warning';
export interface PrerequisiteCheck { id: string; name: string; description: string; status: PrerequisiteStatus; result: {...} }
export interface PrerequisiteResult { allPassed: boolean; checks: PrerequisiteCheck[] }
export interface InstallProgress { correlationId: string; step: number; totalSteps: number; stepName: string; progress: number; overallProgress: number; estimatedTimeRemaining?: number }
export interface InstallError { correlationId: string; message: string; details?: string; step: number }
export type InstallStatus = 'pending' | 'running' | 'paused' | 'failed' | 'completed' | 'rolled_back';
export interface InstallState { target: InstallTarget; status: InstallStatus; currentStep: number; totalSteps: number; stepName?: string; errorMessage?: string; completedSteps: number[] }
```

From src/shared/install/install-channels.ts:
```typescript
export const INSTALL_CHANNELS = {
  start: 'install:v1:start',
  cancel: 'install:v1:cancel',
  retry: 'install:v1:retry',
  getState: 'install:v1:getState',
  runPrerequisites: 'install:v1:runPrerequisites',
  progress: 'install:v1:progress',
  error: 'install:v1:error',
  complete: 'install:v1:complete',
} as const;
```

From src/main/install/prerequisite-service.ts:
```typescript
export async function runAllPrerequisiteChecks(): Promise<PrerequisiteResult>;
```

From src/main/install/install-state-service.ts:
```typescript
export function saveInstallState(state: InstallState): void;
export function loadInstallState(): InstallState | null;
export function clearInstallState(): void;
```

<!-- From Plan 02: Wizard store and UI -->
From src/renderer/stores/wizard-store.ts:
```typescript
export const useWizardStore: UseBoundStore<...>;
// Actions: updateInstallProgress, updatePrerequisite, setAllPrerequisitesPassed, setPrerequisitesLoading
```

From src/main/runtime/process-runner.ts (Phase 1):
```typescript
export async function runProcess(options: RunProcessOptions): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create rollback service with artifact tracking</name>
  <files>src/main/install/rollback-service.ts, src/main/install/__tests__/rollback-service.test.ts</files>
  <read_first>
    - .planning/phases/02-guided-setup-and-install-flows/02-RESEARCH.md (rollback service pattern)
    - src/shared/install/install-contracts.ts (types)
  </read_first>
  <behavior>
    - track(artifact) adds artifact to internal list
    - getSummary() returns list of tracked artifact paths
    - rollback() removes artifacts in reverse order (LIFO)
    - rollback() continues on individual removal errors (best effort)
    - rollback() returns list of successfully removed paths
    - clear() empties artifact list without removing files
  </behavior>
  <action>
Create rollback service for artifact tracking per D-15:

1. `src/main/install/__tests__/rollback-service.test.ts`:
   - Test track() adds artifact to list
   - Test getSummary() returns all tracked paths
   - Test rollback() removes files in reverse order
   - Test rollback() handles missing files gracefully
   - Test clear() empties list without file operations

2. `src/main/install/rollback-service.ts`:
   - Import fs/promises (rm, unlink)
   
   Interface `InstallArtifact`:
   - type: 'file' | 'directory' | 'symlink'
   - path: string
   - createdAt: string
   
   Class `RollbackService`:
   - private artifacts: InstallArtifact[] = []
   
   `track(artifact: InstallArtifact)`:
   - Push to artifacts array
   
   `getSummary()`: string[]
   - Return artifacts.map(a => a.path)
   
   async `rollback()`: Promise<string[]>
   - Reverse copy of artifacts
   - For each: try rm (recursive for directories) or unlink
   - Catch and log errors, continue
   - Clear artifacts after
   - Return list of removed paths
   
   `clear()`:
   - Set artifacts = []
   
   Export singleton instance: `export const rollbackService = new RollbackService()`
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="rollback-service" --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - File src/main/install/rollback-service.ts exports RollbackService class
    - File src/main/install/rollback-service.ts exports rollbackService singleton
    - grep -q "reverse()" src/main/install/rollback-service.ts returns 0 (LIFO order)
    - Test file exists at src/main/install/__tests__/rollback-service.test.ts
    - npm test runs without errors
  </acceptance_criteria>
  <done>Rollback service tracks artifacts and removes them in reverse order on cancel/failure per D-15.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create install orchestrator with step execution</name>
  <files>src/main/install/install-orchestrator.ts, src/main/install/install-steps.ts, src/main/install/__tests__/install-orchestrator.test.ts</files>
  <read_first>
    - src/main/install/rollback-service.ts (just created)
    - src/main/install/install-state-service.ts (from Plan 01)
    - src/main/runtime/process-runner.ts (Phase 1 process execution)
    - .planning/phases/02-guided-setup-and-install-flows/02-RESEARCH.md (orchestrator patterns)
  </read_first>
  <behavior>
    - start(target, correlationId) begins install for openclaw or nemoclaw
    - Progress events emitted via callback with step info (D-09)
    - State saved to SQLite after each step completion (D-16)
    - On error, saves failed state and stops
    - retry(correlationId) resumes from failed step, skips completed (D-13)
    - cancel(correlationId) triggers rollback and returns removed artifacts (D-14)
    - getState() returns current install state from DB
  </behavior>
  <action>
Create install orchestrator and step definitions:

1. `src/main/install/install-steps.ts`:
   Define install step configurations:
   ```typescript
   interface InstallStepConfig {
     id: number;
     name: string;
     description: string;
     execute: (target: InstallTarget, onProgress: ProgressCallback) => Promise<void>;
   }
   
   export const INSTALL_STEPS: InstallStepConfig[] = [
     { id: 1, name: 'Preparing environment', ... },
     { id: 2, name: 'Downloading', ... },
     { id: 3, name: 'Extracting files', ... },
     { id: 4, name: 'Installing dependencies', ... },
     { id: 5, name: 'Finalizing', ... },
   ];
   ```
   
   For now, implement stub execute functions that:
   - Simulate progress with delays
   - Track artifacts via rollbackService.track()
   - Would call process-runner for real operations (placeholder)

2. `src/main/install/__tests__/install-orchestrator.test.ts`:
   - Test start() emits progress events for each step
   - Test start() saves state after each step
   - Test cancel() calls rollback and clears state
   - Test retry() skips completed steps
   - Test getState() returns current state

3. `src/main/install/install-orchestrator.ts`:
   - Import BrowserWindow from electron (for webContents.send)
   - Import INSTALL_CHANNELS from shared
   - Import saveInstallState, loadInstallState, clearInstallState
   - Import rollbackService
   - Import INSTALL_STEPS
   
   Type `ProgressCallback = (progress: InstallProgress) => void`
   
   Class `InstallOrchestrator`:
   - private correlationId: string | null = null
   - private target: InstallTarget | null = null
   - private cancelled = false
   - private window: BrowserWindow | null = null
   
   `setWindow(win: BrowserWindow)`:
   - Store reference for event emission
   
   private `emitProgress(progress: InstallProgress)`:
   - window?.webContents.send(INSTALL_CHANNELS.progress, progress)
   
   private `emitError(error: InstallError)`:
   - window?.webContents.send(INSTALL_CHANNELS.error, error)
   
   private `emitComplete()`:
   - window?.webContents.send(INSTALL_CHANNELS.complete)
   
   async `start(target: InstallTarget, correlationId: string)`:
   - Set this.correlationId, this.target, this.cancelled = false
   - Load existing state to find completedSteps (for retry)
   - Loop through INSTALL_STEPS:
     - Skip if step.id in completedSteps
     - Check this.cancelled before each step
     - Save state with status='running', currentStep=step.id
     - Emit progress
     - Await step.execute()
     - Add step.id to completedSteps
     - Save state with updated completedSteps
   - On success: save status='completed', emitComplete, clearInstallState
   - On error: save status='failed' with error, emitError
   
   async `cancel()`: Promise<{ removed: string[] }>
   - Set this.cancelled = true
   - Call rollbackService.rollback()
   - Save state with status='rolled_back'
   - clearInstallState()
   - Return { removed }
   
   async `retry()`:
   - Load state from DB
   - Call start() with same target/correlationId (completedSteps will be loaded)
   
   `getState()`: InstallState | null
   - Return loadInstallState()
   
   Export singleton: `export const installOrchestrator = new InstallOrchestrator()`
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="install-orchestrator" --passWithNoTests && grep -q "completedSteps" src/main/install/install-orchestrator.ts</automated>
  </verify>
  <acceptance_criteria>
    - File src/main/install/install-orchestrator.ts exports InstallOrchestrator class
    - File src/main/install/install-orchestrator.ts exports installOrchestrator singleton
    - grep -q "emitProgress" src/main/install/install-orchestrator.ts returns 0
    - grep -q "saveInstallState" src/main/install/install-orchestrator.ts returns 0
    - grep -q "completedSteps" src/main/install/install-orchestrator.ts returns 0 (D-13 skip logic)
    - File src/main/install/install-steps.ts exports INSTALL_STEPS array with 5 steps
    - Test file exists at src/main/install/__tests__/install-orchestrator.test.ts
    - npm test runs without errors
  </acceptance_criteria>
  <done>Install orchestrator executes steps with progress events, persists state per D-16, and supports retry from failure point per D-13.</done>
</task>

<task type="auto">
  <name>Task 3: Wire IPC router and preload bridge for install operations</name>
  <files>src/main/ipc/install-router.ts, src/preload/install-api.ts, src/preload/index.ts, src/main/install/index.ts</files>
  <read_first>
    - src/main/ipc/platform-router.ts (existing IPC router pattern)
    - src/preload/platform-api.ts (existing preload pattern)
    - src/shared/install/install-channels.ts (channel constants)
    - src/main/install/install-orchestrator.ts (just created)
    - src/main/install/prerequisite-service.ts (from Plan 01)
  </read_first>
  <action>
Wire IPC layer following Phase 1 patterns:

1. `src/main/ipc/install-router.ts`:
   - Import ipcMain, IpcMainInvokeEvent, BrowserWindow from electron
   - Import INSTALL_CHANNELS from shared/install
   - Import installOrchestrator from main/install
   - Import runAllPrerequisiteChecks from main/install
   - Import zod schemas from shared/install/install-channels
   
   export function `registerInstallHandlers(ipc: typeof ipcMain, mainWindow: BrowserWindow)`:
   
   Set window on orchestrator:
   - installOrchestrator.setWindow(mainWindow)
   
   Register handlers:
   - INSTALL_CHANNELS.start: Validate with schema, call orchestrator.start(target, correlationId), return { correlationId }
   - INSTALL_CHANNELS.cancel: Call orchestrator.cancel(), return { removed }
   - INSTALL_CHANNELS.retry: Call orchestrator.retry(), return void
   - INSTALL_CHANNELS.getState: Call orchestrator.getState(), return state
   - INSTALL_CHANNELS.runPrerequisites: Call runAllPrerequisiteChecks(), return result

2. `src/preload/install-api.ts`:
   - Import contextBridge, ipcRenderer from electron
   - Import INSTALL_CHANNELS from shared/install
   
   const installAPI = {
     start: (target: 'openclaw' | 'nemoclaw'): Promise<{ correlationId: string }> =>
       ipcRenderer.invoke(INSTALL_CHANNELS.start, { target, correlationId: crypto.randomUUID() }),
     
     cancel: (): Promise<{ removed: string[] }> =>
       ipcRenderer.invoke(INSTALL_CHANNELS.cancel),
     
     retry: (): Promise<void> =>
       ipcRenderer.invoke(INSTALL_CHANNELS.retry),
     
     getState: (): Promise<InstallState | null> =>
       ipcRenderer.invoke(INSTALL_CHANNELS.getState),
     
     runPrerequisites: (): Promise<PrerequisiteResult> =>
       ipcRenderer.invoke(INSTALL_CHANNELS.runPrerequisites),
     
     // Event subscriptions - return unsubscribe function
     onProgress: (callback: (progress: InstallProgress) => void) => {
       const handler = (_: unknown, data: InstallProgress) => callback(data);
       ipcRenderer.on(INSTALL_CHANNELS.progress, handler);
       return () => ipcRenderer.removeListener(INSTALL_CHANNELS.progress, handler);
     },
     
     onError: (callback: (error: InstallError) => void) => {
       const handler = (_: unknown, data: InstallError) => callback(data);
       ipcRenderer.on(INSTALL_CHANNELS.error, handler);
       return () => ipcRenderer.removeListener(INSTALL_CHANNELS.error, handler);
     },
     
     onComplete: (callback: () => void) => {
       const handler = () => callback();
       ipcRenderer.on(INSTALL_CHANNELS.complete, handler);
       return () => ipcRenderer.removeListener(INSTALL_CHANNELS.complete, handler);
     },
   };
   
   export { installAPI };

3. `src/preload/index.ts`:
   - Import installAPI from './install-api'
   - Add to contextBridge: window.secureClaw.install = installAPI
   - Update Window interface declaration

4. Update `src/main/install/index.ts`:
   - Re-export from install-orchestrator
   - Re-export from rollback-service
   - Re-export from install-steps
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "registerInstallHandlers" src/main/ipc/install-router.ts && grep -q "installAPI" src/preload/install-api.ts</automated>
  </verify>
  <acceptance_criteria>
    - File src/main/ipc/install-router.ts exports registerInstallHandlers function
    - grep -q "INSTALL_CHANNELS.start" src/main/ipc/install-router.ts returns 0
    - grep -q "runAllPrerequisiteChecks" src/main/ipc/install-router.ts returns 0
    - File src/preload/install-api.ts exports installAPI object
    - grep -q "onProgress" src/preload/install-api.ts returns 0 (event subscription)
    - grep -q "removeListener" src/preload/install-api.ts returns 0 (cleanup)
    - src/preload/index.ts includes install in window.secureClaw
    - TypeScript compiles: npx tsc --noEmit returns 0
  </acceptance_criteria>
  <done>IPC handlers registered for install start/cancel/retry/prerequisites. Preload bridge exposes typed API with event subscriptions and cleanup.</done>
</task>

<task type="auto">
  <name>Task 4: Create renderer hooks and update wizard steps</name>
  <files>
    src/renderer/hooks/use-install-progress.ts,
    src/renderer/hooks/use-prerequisites.ts,
    src/renderer/pages/wizard/PrerequisitesStep.tsx,
    src/renderer/pages/wizard/InstallStep.tsx
  </files>
  <read_first>
    - src/preload/install-api.ts (just created)
    - src/renderer/stores/wizard-store.ts (from Plan 02)
    - src/renderer/pages/wizard/PrerequisitesStep.tsx (from Plan 02)
    - src/renderer/pages/wizard/InstallStep.tsx (from Plan 02)
    - .planning/phases/02-guided-setup-and-install-flows/02-RESEARCH.md (useInstallProgress hook pattern)
  </read_first>
  <action>
Create hooks and wire up wizard steps:

1. `src/renderer/hooks/use-prerequisites.ts`:
   - Import useWizardStore
   - Import useCallback, useEffect from react
   
   export function `usePrerequisites()`:
   - Get setPrerequisitesLoading, updatePrerequisite, setAllPrerequisitesPassed from store
   
   const runChecks = useCallback(async () => {
     setPrerequisitesLoading(true)
     try {
       const result = await window.secureClaw.install.runPrerequisites()
       result.checks.forEach(check => updatePrerequisite(check.id, check))
       setAllPrerequisitesPassed(result.allPassed)
     } finally {
       setPrerequisitesLoading(false)
     }
   }, [])
   
   Return { runChecks }

2. `src/renderer/hooks/use-install-progress.ts`:
   - Import useEffect from react
   - Import useWizardStore
   
   export function `useInstallProgress()`:
   - Get updateInstallProgress, appendLog from store
   
   useEffect(() => {
     const unsubProgress = window.secureClaw.install.onProgress((progress) => {
       updateInstallProgress({
         status: 'running',
         currentStep: progress.step,
         totalSteps: progress.totalSteps,
         stepName: progress.stepName,
         progress: progress.overallProgress,
         estimatedTimeRemaining: progress.estimatedTimeRemaining,
       });
     });
     
     const unsubError = window.secureClaw.install.onError((error) => {
       updateInstallProgress({
         status: 'failed',
         error: { message: error.message, details: error.details },
       });
     });
     
     const unsubComplete = window.secureClaw.install.onComplete(() => {
       updateInstallProgress({ status: 'completed', progress: 100 });
     });
     
     return () => { unsubProgress(); unsubError(); unsubComplete(); };
   }, [updateInstallProgress, appendLog]);

3. Update `src/renderer/pages/wizard/PrerequisitesStep.tsx`:
   - Import usePrerequisites hook
   - Import useEffect
   
   Call usePrerequisites() to get runChecks
   
   useEffect(() => { runChecks(); }, [runChecks]); // Auto-run on mount (D-08)
   
   Wire "Re-check" button onClick to runChecks

4. Update `src/renderer/pages/wizard/InstallStep.tsx`:
   - Import useInstallProgress hook
   - Import useState for cancel dialog
   - Import Dialog from shadcn
   
   Call useInstallProgress() // Sets up IPC subscriptions
   
   Add state for cancel dialog: const [showCancelDialog, setShowCancelDialog] = useState(false)
   
   const handleStartInstall = async () => {
     const target = useWizardStore.getState().selectedTarget;
     if (!target) return;
     await window.secureClaw.install.start(target);
   };
   
   const handleCancel = async () => {
     const { removed } = await window.secureClaw.install.cancel();
     setShowCancelDialog(false);
     // Show what was removed
   };
   
   const handleRetry = async () => {
     await window.secureClaw.install.retry();
   };
   
   Add Dialog for cancel confirmation per D-14:
   - Title: "Cancel Installation"
   - Body: Copywriting from UI-SPEC: "This will remove downloaded files and return to setup. Continue?"
   - Actions: Cancel (close dialog), Confirm (handleCancel)
   
   Wire buttons:
   - "Install" button (if idle) → handleStartInstall
   - "Cancel Installation" button (if running) → setShowCancelDialog(true)
   - "Retry" button in ErrorPanel → handleRetry
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "useInstallProgress" src/renderer/hooks/use-install-progress.ts && grep -q "runPrerequisites" src/renderer/hooks/use-prerequisites.ts</automated>
  </verify>
  <acceptance_criteria>
    - File src/renderer/hooks/use-install-progress.ts exports useInstallProgress hook
    - grep -q "onProgress" src/renderer/hooks/use-install-progress.ts returns 0
    - grep -q "removeListener" or cleanup returns in useInstallProgress (memory leak prevention)
    - File src/renderer/hooks/use-prerequisites.ts exports usePrerequisites hook
    - grep -q "runPrerequisites" src/renderer/hooks/use-prerequisites.ts returns 0
    - PrerequisitesStep.tsx calls runChecks on mount (useEffect)
    - PrerequisitesStep.tsx has Re-check button wired to runChecks
    - InstallStep.tsx imports Dialog from shadcn
    - InstallStep.tsx has cancel confirmation dialog per D-14
    - grep -q "handleRetry" src/renderer/pages/wizard/InstallStep.tsx returns 0
    - TypeScript compiles: npx tsc --noEmit returns 0
  </acceptance_criteria>
  <done>Renderer hooks subscribe to IPC events with proper cleanup. Wizard steps trigger prerequisite checks and install operations through preload bridge. Cancel confirmation dialog implemented per D-14.</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors: `npx tsc --noEmit`
- All tests pass: `npm test`
- IPC handlers registered: `grep -l "registerInstallHandlers" src/main/ipc/*.ts`
- Event subscriptions have cleanup: `grep -c "removeListener" src/preload/install-api.ts` (should be 3)
- Rollback uses LIFO order: `grep -q "reverse()" src/main/install/rollback-service.ts`
- Retry skips completed steps: `grep -q "completedSteps" src/main/install/install-orchestrator.ts`
</verification>

<success_criteria>
- Prerequisite checks can be triggered from UI via IPC (D-08)
- Install can be started with one click, showing live progress (SETUP-03, D-09)
- Progress events stream from main to renderer via webContents.send
- Cancel triggers rollback and shows confirmation dialog (D-14, D-15)
- Retry resumes from failed step, skipping completed (D-13)
- Install state persists in SQLite for crash recovery (D-16)
- Event subscriptions cleaned up in useEffect return (prevents memory leaks)
- All code has test coverage
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/02-guided-setup-and-install-flows/02-03-SUMMARY.md`
</output>
