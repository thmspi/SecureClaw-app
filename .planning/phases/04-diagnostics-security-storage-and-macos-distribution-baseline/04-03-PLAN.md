---
phase: 04-diagnostics-security-storage-and-macos-distribution-baseline
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/main/security/secret-store-service.ts
  - src/main/security/secret-store-service.test.ts
  - src/main/ipc/security-router.ts
  - src/main/ipc/security-router.test.ts
  - src/main/install/install-orchestrator.ts
  - src/main/install/__tests__/install-orchestrator.test.ts
autonomous: true
requirements: [SEC-01, DIAG-01]
must_haves:
  truths:
    - "Sensitive values are stored through an OS-backed secure storage service, not plaintext."
    - "Secret operations are only reachable through narrow typed IPC methods."
    - "When secure storage is unavailable, secret-dependent actions are blocked with remediation guidance."
    - "Secret keys are deleted for uninstall/reset flows."
  artifacts:
    - path: "src/main/security/secret-store-service.ts"
      provides: "safeStorage-backed scoped secret set/get/delete/deleteScope operations."
    - path: "src/main/ipc/security-router.ts"
      provides: "Typed IPC handlers for `secrets:v1:*` channels."
    - path: "src/main/install/install-orchestrator.ts"
      provides: "Secret cleanup integration during cancel/uninstall reset flows."
  key_links:
    - from: "src/main/ipc/security-router.ts"
      to: "src/main/security/secret-store-service.ts"
      via: "validated payload dispatch."
      pattern: "secretStore"
    - from: "src/main/install/install-orchestrator.ts"
      to: "src/main/security/secret-store-service.ts"
      via: "deleteScope('secureclaw') on cancel/uninstall."
      pattern: "deleteScope"
---

<objective>
Implement OS-backed secure secret storage with typed IPC and lifecycle cleanup hooks.

Purpose: Satisfy SEC-01 and D-13..D-16 without expanding renderer privileges.
Output: Secret store service, security IPC router, and uninstall/reset cleanup wiring.
</objective>

<execution_context>
@/Users/thomas/.codex/get-shit-done/workflows/execute-plan.md
@/Users/thomas/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-diagnostics-security-storage-and-macos-distribution-baseline/04-CONTEXT.md
@.planning/phases/04-diagnostics-security-storage-and-macos-distribution-baseline/04-RESEARCH.md
@.planning/phases/04-diagnostics-security-storage-and-macos-distribution-baseline/04-01-PLAN.md
@src/main/install/install-orchestrator.ts
@src/main/install/__tests__/install-orchestrator.test.ts
@src/main/runtime/runtime-history-service.ts
@src/main/ipc/runtime-router.ts
@src/shared/ipc/security-channels.ts
@src/shared/security/secret-contracts.ts

<interfaces>
From src/shared/security/secret-contracts.ts:
```typescript
export type SecretScope = 'runtime' | 'install' | 'plugin' | 'support';
export interface SecretStoreError { userMessage: string; nextSteps: string[]; retryable: boolean; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build safeStorage-backed secret store service with scoped key rotation semantics</name>
  <read_first>
    src/main/runtime/runtime-history-service.ts
    src/shared/security/secret-contracts.ts
    src/main/security/secret-store-service.test.ts
    .planning/phases/04-diagnostics-security-storage-and-macos-distribution-baseline/04-CONTEXT.md
  </read_first>
  <files>src/main/security/secret-store-service.ts, src/main/security/secret-store-service.test.ts</files>
  <behavior>
    - Test 1: set/get/delete/deleteScope round-trip works for `secureclaw:{scope}:{name}` keys.
    - Test 2: overwrite increments version (rotation) and returns latest value only.
    - Test 3: unavailable safeStorage returns typed remediation error and no plaintext fallback.
  </behavior>
  <action>
    Implement `secret-store-service.ts` using Electron `safeStorage` and `better-sqlite3` table `secret_store` with columns:
    `scoped_key TEXT PRIMARY KEY`, `encrypted_value BLOB NOT NULL`, `version INTEGER NOT NULL`, `updated_at TEXT NOT NULL`.
    Enforce scoped key format exactly: `secureclaw:{scope}:{name}` (D-16).
    On overwrite, increment `version = version + 1` and replace encrypted value.
    If `safeStorage.isEncryptionAvailable()` is false, return `SecretStoreError` with next steps:
    `Unlock macOS Keychain`, `Grant SecureClaw keychain access`, `Export diagnostics from Settings > Health and share with IT`.
    Do not persist plaintext secret values anywhere.
  </action>
  <verify>
    <automated>npm test -- secret-store-service.test.ts --runInBand --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "safeStorage.isEncryptionAvailable\\(" src/main/security/secret-store-service.ts` returns one match.
    - `rg -n "secureclaw:\\$\\{scope\\}:\\$\\{name\\}" src/main/security/secret-store-service.ts` returns one match.
    - `rg -n "version INTEGER NOT NULL" src/main/security/secret-store-service.ts` returns one match.
    - `rg -n "Unlock macOS Keychain" src/main/security/secret-store-service.ts` returns one match.
  </acceptance_criteria>
  <done>Secret service enforces OS-backed encrypted storage with explicit unavailable-state remediation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose secret operations through typed `secrets:v1:*` IPC router</name>
  <read_first>
    src/main/ipc/runtime-router.ts
    src/shared/ipc/security-channels.ts
    src/main/ipc/security-router.test.ts
    src/main/security/secret-store-service.ts
  </read_first>
  <files>src/main/ipc/security-router.ts, src/main/ipc/security-router.test.ts</files>
  <behavior>
    - Test 1: router validates zod payload schemas before invoking secret store.
    - Test 2: router handles set/get/delete/deleteScope channels with typed responses.
    - Test 3: router returns plain-language error envelope payload, not raw thrown string.
  </behavior>
  <action>
    Implement `registerSecurityHandlers(ipcMain)` in `src/main/ipc/security-router.ts` with handlers:
    `secrets:v1:set`, `secrets:v1:get`, `secrets:v1:delete`, `secrets:v1:deleteScope`.
    Validate all requests with zod schemas from `src/shared/ipc/security-channels.ts`.
    Forward to secret-store service methods only; do not expose any raw DB or crypto API.
    Map failures to typed `SecretStoreError` with `userMessage`, `nextSteps`, `retryable`, and `technicalDetails` (D-02, D-15).
  </action>
  <verify>
    <automated>npm test -- security-router.test.ts --runInBand --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "secrets:v1:set" src/main/ipc/security-router.ts` returns one match.
    - `rg -n "secrets:v1:get" src/main/ipc/security-router.ts` returns one match.
    - `rg -n "secrets:v1:deleteScope" src/main/ipc/security-router.ts` returns one match.
    - `rg -n "userMessage" src/main/ipc/security-router.ts` returns at least one match.
  </acceptance_criteria>
  <done>Security router provides narrow typed IPC access to secret operations only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire secret cleanup into uninstall/reset lifecycle paths</name>
  <read_first>
    src/main/install/install-orchestrator.ts
    src/main/install/__tests__/install-orchestrator.test.ts
    src/main/security/secret-store-service.ts
    .planning/phases/04-diagnostics-security-storage-and-macos-distribution-baseline/04-CONTEXT.md
  </read_first>
  <files>src/main/install/install-orchestrator.ts, src/main/install/__tests__/install-orchestrator.test.ts</files>
  <behavior>
    - Test 1: cancel flow invokes scope cleanup before returning.
    - Test 2: uninstall stack flow invokes scope cleanup and still returns removed/errors payload.
  </behavior>
  <action>
    Update `InstallOrchestrator.cancel()` and `InstallOrchestrator.uninstallStack()` to call
    secret store scope cleanup using exact scope prefix `secureclaw:` and scope set `install`, `runtime`, `plugin`, `support`.
    Keep existing rollback/uninstall behavior intact.
    If cleanup fails, append a warning string into `errors` output and log technical detail for diagnostics export.
    Do not silently swallow secret cleanup failures.
  </action>
  <verify>
    <automated>npm test -- install-orchestrator.test.ts --runInBand --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "deleteScope" src/main/install/install-orchestrator.ts` returns at least one match.
    - `rg -n "secureclaw:" src/main/install/install-orchestrator.ts` returns at least one match.
    - `rg -n "secret" src/main/install/__tests__/install-orchestrator.test.ts` returns at least one match.
  </acceptance_criteria>
  <done>Secret cleanup executes in uninstall/reset lifecycle paths per D-16.</done>
</task>

</tasks>

<verification>
Run `npm test -- secret-store-service.test.ts security-router.test.ts install-orchestrator.test.ts --runInBand --passWithNoTests` and `npm run type-check`.
</verification>

<success_criteria>
- Secrets never persist in plaintext and are retrievable only through typed IPC.
- Unavailable secure storage blocks writes with user-facing remediation steps.
- Uninstall/reset cleanup explicitly deletes scoped secrets.
</success_criteria>

<output>
After completion, create `.planning/phases/04-diagnostics-security-storage-and-macos-distribution-baseline/04-03-SUMMARY.md`
</output>

