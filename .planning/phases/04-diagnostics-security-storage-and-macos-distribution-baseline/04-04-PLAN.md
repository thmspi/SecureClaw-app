---
phase: 04-diagnostics-security-storage-and-macos-distribution-baseline
plan: 04
type: execute
wave: 3
depends_on: [04-02, 04-03]
files_modified:
  - src/main/index.ts
  - src/preload/platform-api.ts
  - src/renderer/stores/management-store.ts
  - src/renderer/pages/management/SettingsPanel.tsx
  - src/renderer/pages/management/SessionControl.tsx
  - src/renderer/pages/management/PluginCatalog.tsx
  - src/renderer/components/management/InlineSupportError.tsx
  - src/preload/__tests__/platform-api.test.ts
autonomous: true
requirements: [DIAG-01, DIAG-02, DIAG-03, SEC-01]
must_haves:
  truths:
    - "Health dashboard is available in Settings and refreshes automatically around every 10 seconds plus manual refresh."
    - "Diagnostics export action is available in Settings > Health and shows completion/failure inline."
    - "Errors in session/plugin/health sections are inline, plain-language, and show Retry only when retryable."
    - "Technical details are visible but collapsed by default."
  artifacts:
    - path: "src/renderer/pages/management/SettingsPanel.tsx"
      provides: "Health dashboard cards, export button, and inline diagnostics feedback."
    - path: "src/renderer/components/management/InlineSupportError.tsx"
      provides: "Reusable inline support error UI with optional retry + collapsible technical details."
    - path: "src/renderer/stores/management-store.ts"
      provides: "Health snapshot state, diagnostics export state, auto-refresh scheduler, and error envelopes."
    - path: "src/preload/platform-api.ts"
      provides: "Narrow diagnostics/secrets preload bridge."
  key_links:
    - from: "src/preload/platform-api.ts"
      to: "src/main/index.ts"
      via: "diagnostics/security handler registration and exposed bridge methods."
      pattern: "diagnostics|secrets"
    - from: "src/renderer/stores/management-store.ts"
      to: "window.secureClaw.diagnostics"
      via: "loadHealth/exportDiagnostics actions."
      pattern: "secureClaw\\.diagnostics"
    - from: "src/renderer/pages/management/SettingsPanel.tsx"
      to: "InlineSupportError"
      via: "inline section-level error rendering."
      pattern: "InlineSupportError"
---

<objective>
Wire diagnostics/security APIs into app runtime and deliver Settings health + diagnostics UX with decision-locked inline remediation behavior.

Purpose: Complete user-facing DIAG requirements while preserving strict IPC boundaries.
Output: Main/preload wiring and renderer settings/session/plugin error UX updates.
</objective>

<execution_context>
@/Users/thomas/.codex/get-shit-done/workflows/execute-plan.md
@/Users/thomas/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-diagnostics-security-storage-and-macos-distribution-baseline/04-CONTEXT.md
@.planning/phases/04-diagnostics-security-storage-and-macos-distribution-baseline/04-02-PLAN.md
@.planning/phases/04-diagnostics-security-storage-and-macos-distribution-baseline/04-03-PLAN.md
@src/main/index.ts
@src/preload/platform-api.ts
@src/renderer/stores/management-store.ts
@src/renderer/pages/management/SettingsPanel.tsx
@src/renderer/pages/management/SessionControl.tsx
@src/renderer/pages/management/PluginCatalog.tsx
@src/renderer/components/wizard/ErrorPanel.tsx

<interfaces>
From src/preload/platform-api.ts:
```typescript
contextBridge.exposeInMainWorld('secureClaw', {
  platform: platformAPI,
  install: installAPI,
  runtime: runtimeAPI,
});
```

From src/renderer/stores/management-store.ts:
```typescript
loadSessions: () => Promise<void>;
loadPluginPackages: () => Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register diagnostics/security handlers and expose narrow preload bridge APIs</name>
  <read_first>
    src/main/index.ts
    src/preload/platform-api.ts
    src/preload/__tests__/platform-api.test.ts
    src/main/ipc/runtime-router.ts
    src/main/ipc/install-router.ts
  </read_first>
  <files>src/main/index.ts, src/preload/platform-api.ts, src/preload/__tests__/platform-api.test.ts</files>
  <behavior>
    - Test 1: main process registers diagnostics and security handlers once during startup.
    - Test 2: renderer receives `window.secureClaw.diagnostics` and `window.secureClaw.secrets` APIs, without exposing raw ipcRenderer.
  </behavior>
  <action>
    Import and register `registerDiagnosticsHandlers` and `registerSecurityHandlers` in `src/main/index.ts` in the same guarded path as existing handler registration.
    Extend preload bridge in `src/preload/platform-api.ts` with exactly two new objects:
    `diagnostics` exposing `getHealth({ forceRefresh? })` and `exportBundle({ includeDays? })`,
    `secrets` exposing `setSecret`, `getSecret`, `deleteSecret`, `deleteScopeSecrets`.
    Keep narrow bridge policy: no raw `ipcRenderer` exposure in renderer-facing types.
  </action>
  <verify>
    <automated>npm test -- src/preload/__tests__/platform-api.test.ts --runInBand --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "registerDiagnosticsHandlers" src/main/index.ts` returns one match.
    - `rg -n "registerSecurityHandlers" src/main/index.ts` returns one match.
    - `rg -n "diagnostics:" src/preload/platform-api.ts` returns at least one match.
    - `rg -n "secrets:" src/preload/platform-api.ts` returns at least one match.
    - `rg -n "NO raw ipcRenderer" src/preload/platform-api.ts` still matches existing narrow-bridge comment context.
  </acceptance_criteria>
  <done>Main/preload wiring is complete for diagnostics/secrets without widening renderer privileges.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend management store with health state, diagnostics export flow, and 10-second auto-refresh</name>
  <read_first>
    src/renderer/stores/management-store.ts
    src/shared/diagnostics/diagnostics-contracts.ts
    src/renderer/pages/management/ManagementPage.tsx
    .planning/phases/04-diagnostics-security-storage-and-macos-distribution-baseline/04-CONTEXT.md
  </read_first>
  <files>src/renderer/stores/management-store.ts</files>
  <behavior>
    - Test 1: store exposes `loadHealth`, `refreshHealth`, `exportDiagnostics`, and `diagnosticsError` state.
    - Test 2: auto-refresh interval is exactly `10000` ms and can be cancelled on unmount.
  </behavior>
  <action>
    Add management-store diagnostics state fields:
    `healthSnapshot`, `healthLoading`, `healthError`, `diagnosticsExporting`, `diagnosticsExportPath`, `diagnosticsError`.
    Add actions:
    `loadHealth(forceRefresh?: boolean)`,
    `startHealthAutoRefresh()` using `window.setInterval(..., 10000)` (per D-08),
    `stopHealthAutoRefresh()`,
    `exportDiagnostics(includeDays = 7)` calling preload diagnostics export.
    Persist NONE of this diagnostics state (keep ephemeral like current runtime state).
    Normalize errors to envelope shape with `userMessage`, `nextSteps`, `retryable`, `technicalDetails`.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "setInterval\\(.*10000" src/renderer/stores/management-store.ts` returns one match.
    - `rg -n "loadHealth" src/renderer/stores/management-store.ts` returns at least one match.
    - `rg -n "exportDiagnostics" src/renderer/stores/management-store.ts` returns at least one match.
    - `rg -n "diagnosticsExportPath" src/renderer/stores/management-store.ts` returns at least one match.
  </acceptance_criteria>
  <done>Store can drive settings health data and diagnostics export with auto/manual refresh behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement inline health/error UI in management settings and runtime sections</name>
  <read_first>
    src/renderer/pages/management/SettingsPanel.tsx
    src/renderer/pages/management/SessionControl.tsx
    src/renderer/pages/management/PluginCatalog.tsx
    src/renderer/components/wizard/ErrorPanel.tsx
    src/renderer/stores/management-store.ts
    .planning/phases/04-diagnostics-security-storage-and-macos-distribution-baseline/04-CONTEXT.md
  </read_first>
  <files>src/renderer/components/management/InlineSupportError.tsx, src/renderer/pages/management/SettingsPanel.tsx, src/renderer/pages/management/SessionControl.tsx, src/renderer/pages/management/PluginCatalog.tsx</files>
  <behavior>
    - Test 1: runtime/management errors render inline in each section with plain-language what-happened + next-steps content.
    - Test 2: Retry button is shown only when `retryable === true` (D-03).
    - Test 3: technical details are collapsed by default and expandable (D-04).
  </behavior>
  <action>
    Create `InlineSupportError` component that renders:
    section title, `userMessage`, ordered `nextSteps`, optional `Retry` button (only when `retryable`), and collapsible `technicalDetails`.
    Update `SettingsPanel` to add a `Health` section under existing Settings with:
    overall severity badge, per-component badges for install/runtime/plugins,
    version rows for `app`, `openclaw`, `nemoclaw`, `docker`,
    `Refresh` button and `Export Diagnostics (.zip)` button (D-05, D-06, D-08, D-12).
    Update `SessionControl` and `PluginCatalog` to render `InlineSupportError` for failures instead of `console.error` or plain strings.
    For runtime string errors, classify retryability concretely:
    retryable when message matches `(timeout|timed out|temporar|EAI_AGAIN|ECONNRESET)`,
    non-retryable when message matches `(ENOENT|not found|EACCES|permission|unsupported)`.
  </action>
  <verify>
    <automated>npm run build:renderer</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Export Diagnostics \\(.zip\\)" src/renderer/pages/management/SettingsPanel.tsx` returns one match.
    - `rg -n "app|openclaw|nemoclaw|docker" src/renderer/pages/management/SettingsPanel.tsx` returns matches for all four labels.
    - `rg -n "retryable" src/renderer/components/management/InlineSupportError.tsx` returns at least one match.
    - `rg -n "Collapsible" src/renderer/components/management/InlineSupportError.tsx` returns at least one match.
    - `rg -n "console\\.error\\('Failed to start session'" src/renderer/pages/management/SessionControl.tsx` returns no matches.
  </acceptance_criteria>
  <done>Settings health + diagnostics UX and inline remediation behavior match D-01 through D-12 constraints.</done>
</task>

</tasks>

<verification>
Run `npm run type-check`, `npm run build:renderer`, and `npm test -- src/preload/__tests__/platform-api.test.ts --runInBand --passWithNoTests`.
</verification>

<success_criteria>
- Health dashboard is visible in Settings with automatic ~10s refresh and manual refresh.
- Diagnostics export is initiated from Settings and reports results inline.
- Retry controls are conditionally rendered only for retryable failures and technical details remain collapsed by default.
</success_criteria>

<output>
After completion, create `.planning/phases/04-diagnostics-security-storage-and-macos-distribution-baseline/04-04-SUMMARY.md`
</output>

