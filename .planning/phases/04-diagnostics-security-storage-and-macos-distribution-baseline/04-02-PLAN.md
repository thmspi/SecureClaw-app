---
phase: 04-diagnostics-security-storage-and-macos-distribution-baseline
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - package.json
  - package-lock.json
  - src/main/diagnostics/redaction.ts
  - src/main/diagnostics/redaction.test.ts
  - src/main/diagnostics/health-service.ts
  - src/main/diagnostics/health-service.test.ts
  - src/main/diagnostics/diagnostics-export-service.ts
  - src/main/diagnostics/diagnostics-export-service.test.ts
  - src/main/ipc/diagnostics-router.ts
  - src/main/ipc/diagnostics-router.test.ts
autonomous: true
requirements: [DIAG-01, DIAG-02, DIAG-03]
must_haves:
  truths:
    - "Settings can request one health snapshot containing install/runtime/plugins + app/openclaw/nemoclaw/docker versions."
    - "Diagnostics export returns a single ZIP bundle with automatic redaction and summary counts."
    - "Diagnostics failures return plain-language remediation with retryability and optional technical details."
  artifacts:
    - path: "src/main/diagnostics/health-service.ts"
      provides: "Main-process health aggregator with worst-of severity model."
    - path: "src/main/diagnostics/diagnostics-export-service.ts"
      provides: "ZIP generation pipeline for support handoff bundle."
    - path: "src/main/diagnostics/redaction.ts"
      provides: "Deterministic secret/path/identifier redaction with summary counters."
    - path: "src/main/ipc/diagnostics-router.ts"
      provides: "Typed IPC handlers for diagnostics:v1:getHealth and diagnostics:v1:exportBundle."
  key_links:
    - from: "src/main/ipc/diagnostics-router.ts"
      to: "src/main/diagnostics/health-service.ts"
      via: "IPC handler invokes getHealthSnapshot."
      pattern: "getHealthSnapshot\\("
    - from: "src/main/ipc/diagnostics-router.ts"
      to: "src/main/diagnostics/diagnostics-export-service.ts"
      via: "IPC handler invokes exportDiagnosticsBundle."
      pattern: "exportDiagnosticsBundle\\("
    - from: "src/main/diagnostics/diagnostics-export-service.ts"
      to: "src/main/diagnostics/redaction.ts"
      via: "redaction pass before archive finalize."
      pattern: "redact"
---

<objective>
Implement the diagnostics backend (health snapshot + redacted support bundle export) behind typed IPC handlers.

Purpose: Deliver DIAG-02 and DIAG-03 while ensuring DIAG-01 plain-language error guidance is available in diagnostics responses.
Output: Main-process diagnostics services, router handlers, tests, and required dependency additions.
</objective>

<execution_context>
@/Users/thomas/.codex/get-shit-done/workflows/execute-plan.md
@/Users/thomas/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-diagnostics-security-storage-and-macos-distribution-baseline/04-CONTEXT.md
@.planning/phases/04-diagnostics-security-storage-and-macos-distribution-baseline/04-RESEARCH.md
@.planning/phases/04-diagnostics-security-storage-and-macos-distribution-baseline/04-01-PLAN.md
@src/main/runtime/runtime-history-service.ts
@src/main/install/install-state-service.ts
@src/main/runtime/session-orchestrator.ts
@src/main/runtime/plugin-catalog-service.ts
@src/main/ipc/runtime-router.ts

<interfaces>
From src/main/runtime/runtime-history-service.ts:
```typescript
export function getHistory(request: GetHistoryRequest): GetHistoryResponse;
```

From src/main/install/install-state-service.ts:
```typescript
export function loadInstallState(): InstallState | null;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add diagnostics dependencies and baseline test scaffolds</name>
  <read_first>
    package.json
    .planning/phases/04-diagnostics-security-storage-and-macos-distribution-baseline/04-RESEARCH.md
    src/main/ipc/runtime-router.test.ts
  </read_first>
  <files>package.json, package-lock.json, src/main/diagnostics/redaction.test.ts, src/main/diagnostics/health-service.test.ts, src/main/diagnostics/diagnostics-export-service.test.ts, src/main/ipc/diagnostics-router.test.ts</files>
  <action>
    Add runtime dependencies exactly as researched: `archiver`, `electron-log`, and `fast-redact`.
    Create test files for redaction, health snapshot, diagnostics export, and diagnostics router before implementation.
    Seed tests with explicit assertions for D-06 component keys, D-11 redaction summary counts, and `diagnostics:v1:*` handler behavior.
  </action>
  <verify>
    <automated>npm install && npm test -- diagnostics-router.test.ts --runInBand --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "\"archiver\"" package.json` returns one match.
    - `rg -n "\"electron-log\"" package.json` returns one match.
    - `rg -n "\"fast-redact\"" package.json` returns one match.
    - `test -f src/main/diagnostics/redaction.test.ts` exits 0.
    - `test -f src/main/ipc/diagnostics-router.test.ts` exits 0.
  </acceptance_criteria>
  <done>Dependencies and diagnostics test scaffolds exist and are runnable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement redaction + diagnostics ZIP export pipeline per D-09 to D-11</name>
  <read_first>
    src/main/runtime/runtime-history-service.ts
    src/main/install/install-state-service.ts
    src/main/diagnostics/redaction.test.ts
    src/main/diagnostics/diagnostics-export-service.test.ts
    .planning/phases/04-diagnostics-security-storage-and-macos-distribution-baseline/04-CONTEXT.md
  </read_first>
  <files>src/main/diagnostics/redaction.ts, src/main/diagnostics/diagnostics-export-service.ts, src/main/diagnostics/redaction.test.ts, src/main/diagnostics/diagnostics-export-service.test.ts</files>
  <behavior>
    - Test 1: text and JSON payloads redact secrets/tokens/paths/user identifiers and report per-matcher counts.
    - Test 2: export writes one `.zip` containing redacted `health-snapshot.json`, `component-versions.json`, `operation-history.json`, and `redaction-summary.json`.
  </behavior>
  <action>
    Implement `src/main/diagnostics/redaction.ts` with deterministic matchers and replacements:
    `Bearer <token>` => `Bearer [REDACTED_TOKEN]`,
    `api_key|token|secret|password` key-values => `[REDACTED_SECRET]`,
    `/Users/<username>` => `/Users/[REDACTED_USER]`,
    email addresses => `[REDACTED_EMAIL]`.
    Expose a redaction summary object with exact keys:
    `tokenMatches`, `secretMatches`, `pathMatches`, `emailMatches`.

    Implement `src/main/diagnostics/diagnostics-export-service.ts` to:
    create a temporary export directory, collect history/install snapshot/log artifacts,
    apply redaction to each artifact, write `redaction-summary.json`,
    and generate a single ZIP bundle using `archiver` (D-09, D-10, D-11).
    Return plain-language errors through `SupportErrorEnvelope` with actionable next steps.
  </action>
  <verify>
    <automated>npm test -- redaction.test.ts diagnostics-export-service.test.ts --runInBand --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "tokenMatches" src/main/diagnostics/redaction.ts` returns one match.
    - `rg -n "redaction-summary.json" src/main/diagnostics/diagnostics-export-service.ts` returns one match.
    - `rg -n "archiver\\('zip'" src/main/diagnostics/diagnostics-export-service.ts` returns one match.
    - `rg -n "operation-history.json" src/main/diagnostics/diagnostics-export-service.ts` returns one match.
  </acceptance_criteria>
  <done>Diagnostics export emits one redacted ZIP with summary counts and tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement health snapshot service + diagnostics IPC router per D-06 to D-08</name>
  <read_first>
    src/main/ipc/runtime-router.ts
    src/main/runtime/session-orchestrator.ts
    src/main/runtime/plugin-catalog-service.ts
    src/main/diagnostics/health-service.test.ts
    src/main/ipc/diagnostics-router.test.ts
    src/shared/ipc/diagnostics-channels.ts
  </read_first>
  <files>src/main/diagnostics/health-service.ts, src/main/ipc/diagnostics-router.ts, src/main/diagnostics/health-service.test.ts, src/main/ipc/diagnostics-router.test.ts</files>
  <behavior>
    - Test 1: health snapshot includes install/runtime/plugins component statuses and app/openclaw/nemoclaw/docker versions.
    - Test 2: overall severity is worst-of component severities (`Healthy` < `Warning` < `Critical`).
    - Test 3: diagnostics router validates payload schema and returns typed errors instead of raw throw strings.
  </behavior>
  <action>
    Implement `getHealthSnapshot()` in `src/main/diagnostics/health-service.ts` using:
    install status from `loadInstallState()`,
    runtime status from `session-orchestrator.getSessions()`,
    plugin subsystem status from plugin package list call success/failure,
    version probes for `app` (`app.getVersion()`), `openclaw`, `nemoclaw`, `docker` (CLI `--version`).
    Compute overall severity with strict order:
    `Critical` if any component critical,
    else `Warning` if any warning,
    else `Healthy` (per D-07).

    Implement `src/main/ipc/diagnostics-router.ts` with handlers for:
    `diagnostics:v1:getHealth` and `diagnostics:v1:exportBundle`,
    zod validation, and plain-language `SupportErrorEnvelope` on failure (D-02).
  </action>
  <verify>
    <automated>npm test -- health-service.test.ts diagnostics-router.test.ts --runInBand --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "diagnostics:v1:getHealth" src/main/ipc/diagnostics-router.ts` returns one match.
    - `rg -n "diagnostics:v1:exportBundle" src/main/ipc/diagnostics-router.ts` returns one match.
    - `rg -n "overallSeverity" src/main/diagnostics/health-service.ts` returns at least one match.
    - `rg -n "app.getVersion\\(" src/main/diagnostics/health-service.ts` returns one match.
  </acceptance_criteria>
  <done>Diagnostics backend is executable via typed IPC and validated by focused tests.</done>
</task>

</tasks>

<verification>
Run `npm run type-check` and `npm test -- diagnostics-router.test.ts health-service.test.ts diagnostics-export-service.test.ts redaction.test.ts --runInBand --passWithNoTests`.
</verification>

<success_criteria>
- `getHealth` returns D-06 component/version payload with D-07 severity model.
- `exportBundle` returns a single redacted ZIP plus redaction summary.
- Diagnostics errors surface plain language + next steps + retryability.
</success_criteria>

<output>
After completion, create `.planning/phases/04-diagnostics-security-storage-and-macos-distribution-baseline/04-02-SUMMARY.md`
</output>

