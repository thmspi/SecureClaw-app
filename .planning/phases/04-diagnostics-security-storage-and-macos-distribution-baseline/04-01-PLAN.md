---
phase: 04-diagnostics-security-storage-and-macos-distribution-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/diagnostics/diagnostics-contracts.ts
  - src/shared/security/secret-contracts.ts
  - src/shared/ipc/diagnostics-channels.ts
  - src/shared/ipc/security-channels.ts
  - src/shared/ipc/index.ts
autonomous: true
requirements: [DIAG-01, DIAG-02, DIAG-03, SEC-01]
must_haves:
  truths:
    - "Diagnostics and secret-storage operations have explicit typed request/response contracts."
    - "Error payloads include plain-language remediation and retryability flags."
    - "Health and export payloads can represent all D-06 required components and versions."
  artifacts:
    - path: "src/shared/diagnostics/diagnostics-contracts.ts"
      provides: "Canonical diagnostics health/export contract types including support error envelope."
    - path: "src/shared/security/secret-contracts.ts"
      provides: "Canonical secure secret storage contract types and scoped key format."
    - path: "src/shared/ipc/diagnostics-channels.ts"
      provides: "Versioned diagnostics channel names and zod schemas."
    - path: "src/shared/ipc/security-channels.ts"
      provides: "Versioned secrets channel names and zod schemas."
  key_links:
    - from: "src/shared/diagnostics/diagnostics-contracts.ts"
      to: "src/shared/ipc/diagnostics-channels.ts"
      via: "Typed payloads enforced by zod schemas."
      pattern: "diagnostics:v1:"
    - from: "src/shared/security/secret-contracts.ts"
      to: "src/shared/ipc/security-channels.ts"
      via: "Scoped key/secret payload schema validation."
      pattern: "secrets:v1:"
---

<objective>
Create the contract-first Phase 4 foundation for diagnostics and secure secret storage so all downstream implementation stays inside typed IPC boundaries.

Purpose: Lock the wire format before implementation to avoid drift across main/preload/renderer.
Output: Shared contract + channel modules for diagnostics and secrets.
</objective>

<execution_context>
@/Users/thomas/.codex/get-shit-done/workflows/execute-plan.md
@/Users/thomas/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-diagnostics-security-storage-and-macos-distribution-baseline/04-CONTEXT.md
@.planning/phases/04-diagnostics-security-storage-and-macos-distribution-baseline/04-RESEARCH.md
@src/shared/ipc/runtime-channels.ts
@src/shared/runtime/runtime-contracts.ts

<interfaces>
From src/shared/ipc/runtime-channels.ts:
```typescript
export const RUNTIME_CHANNELS = {
  startSession: 'runtime:v1:startSession',
  stopSession: 'runtime:v1:stopSession',
  getSessions: 'runtime:v1:getSessions',
  getHistory: 'runtime:v1:getHistory',
} as const;
```

From src/shared/runtime/runtime-contracts.ts:
```typescript
export type OperationType = 'session_start' | 'session_stop' | 'plugin_run';
export type OperationStatus = 'success' | 'failed' | 'cancelled';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define diagnostics contracts with plain-language error envelope and health snapshot shape</name>
  <read_first>
    src/shared/runtime/runtime-contracts.ts
    src/shared/ipc/runtime-channels.ts
    .planning/phases/04-diagnostics-security-storage-and-macos-distribution-baseline/04-CONTEXT.md
  </read_first>
  <files>src/shared/diagnostics/diagnostics-contracts.ts</files>
  <behavior>
    - Test 1: health contract includes install/runtime/plugins + versions for app/openclaw/nemoclaw/docker.
    - Test 2: diagnostics export contract includes zip path, redaction summary counts, and plain-language error envelope.
  </behavior>
  <action>
    Create `src/shared/diagnostics/diagnostics-contracts.ts` with exact exported types:
    `HealthSeverity = 'Healthy' | 'Warning' | 'Critical'` (per D-07),
    `HealthComponentId = 'install' | 'runtime' | 'plugins'`,
    `VersionKey = 'app' | 'openclaw' | 'nemoclaw' | 'docker'`,
    `SupportErrorEnvelope` containing `userMessage`, `nextSteps: string[]`, `retryable`, `technicalDetails?`, `errorCode`,
    `HealthSnapshot` containing `overallSeverity`, `components`, `versions`, `generatedAt`,
    and `DiagnosticsExportResult` containing `bundlePath`, `generatedAt`, `redactionSummary`.
    Ensure D-02 and D-04 are represented by `userMessage` + optional `technicalDetails`.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "export type HealthSeverity = 'Healthy' \\| 'Warning' \\| 'Critical'" src/shared/diagnostics/diagnostics-contracts.ts` returns one match.
    - `rg -n "export interface SupportErrorEnvelope" src/shared/diagnostics/diagnostics-contracts.ts` returns one match.
    - `rg -n "versions: Record<VersionKey, string \\| null>" src/shared/diagnostics/diagnostics-contracts.ts` returns one match.
  </acceptance_criteria>
  <done>Diagnostics contracts compile and encode D-02, D-04, D-06, and D-07 as explicit types.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define secure secret storage contracts with scoped key lifecycle semantics</name>
  <read_first>
    src/shared/install/install-contracts.ts
    .planning/phases/04-diagnostics-security-storage-and-macos-distribution-baseline/04-CONTEXT.md
    .planning/phases/04-diagnostics-security-storage-and-macos-distribution-baseline/04-RESEARCH.md
  </read_first>
  <files>src/shared/security/secret-contracts.ts</files>
  <behavior>
    - Test 1: set/get/delete/deleteScope payloads require scoped keys and return typed success/error results.
    - Test 2: secure-storage-unavailable response includes remediation steps and diagnostics guidance.
  </behavior>
  <action>
    Create `src/shared/security/secret-contracts.ts` with exact exported structures:
    `SecretScope = 'runtime' | 'install' | 'plugin' | 'support'`,
    `SecretScopedKey` format rule documented as `secureclaw:{scope}:{name}` (per D-16),
    request/response interfaces for `SetSecret`, `GetSecret`, `DeleteSecret`, and `DeleteScopeSecrets`,
    and `SecretStoreError` carrying `userMessage`, `nextSteps`, `retryable`, `technicalDetails?`, `errorCode`.
    Include explicit `secureStorageAvailable: boolean` in responses so callers can block secret-dependent flows (D-15).
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "secureclaw:\\{scope\\}:\\{name\\}" src/shared/security/secret-contracts.ts` returns one match.
    - `rg -n "export interface DeleteScopeSecretsRequest" src/shared/security/secret-contracts.ts` returns one match.
    - `rg -n "secureStorageAvailable: boolean" src/shared/security/secret-contracts.ts` returns at least one match.
  </acceptance_criteria>
  <done>Secret contract module explicitly models D-13 through D-16 and compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add versioned diagnostics/secrets IPC channels and zod validation schemas</name>
  <read_first>
    src/shared/ipc/runtime-channels.ts
    src/shared/ipc/platform-channels.ts
    src/shared/ipc/index.ts
    src/shared/diagnostics/diagnostics-contracts.ts
    src/shared/security/secret-contracts.ts
  </read_first>
  <files>src/shared/ipc/diagnostics-channels.ts, src/shared/ipc/security-channels.ts, src/shared/ipc/index.ts</files>
  <behavior>
    - Test 1: diagnostics channels expose `diagnostics:v1:getHealth` and `diagnostics:v1:exportBundle`.
    - Test 2: secrets channels expose `secrets:v1:set/get/delete/deleteScope` and validate payload schema shape.
  </behavior>
  <action>
    Create `src/shared/ipc/diagnostics-channels.ts` with `DIAGNOSTICS_CHANNELS` constants:
    `diagnostics:v1:getHealth` and `diagnostics:v1:exportBundle`.
    Add zod schemas:
    `getHealthSchema = z.object({ forceRefresh: z.boolean().optional() })`,
    `exportBundleSchema = z.object({ includeDays: z.number().int().positive().max(30).default(7) })`.

    Create `src/shared/ipc/security-channels.ts` with `SECURITY_CHANNELS` constants:
    `secrets:v1:set`, `secrets:v1:get`, `secrets:v1:delete`, `secrets:v1:deleteScope`.
    Add zod schemas with explicit string lengths:
    `scope` min 1, `name` min 1, `value` min 1.
    Update `src/shared/ipc/index.ts` to export both new channel modules.
    Use D-14 channel versioning and narrow method scope exactly.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "diagnostics:v1:getHealth" src/shared/ipc/diagnostics-channels.ts` returns one match.
    - `rg -n "diagnostics:v1:exportBundle" src/shared/ipc/diagnostics-channels.ts` returns one match.
    - `rg -n "secrets:v1:set" src/shared/ipc/security-channels.ts` returns one match.
    - `rg -n "secrets:v1:deleteScope" src/shared/ipc/security-channels.ts` returns one match.
    - `rg -n "export \\* from './diagnostics-channels'" src/shared/ipc/index.ts` returns one match.
  </acceptance_criteria>
  <done>Diagnostics and secrets IPC channels are versioned, typed, and exported for main/preload usage.</done>
</task>

</tasks>

<verification>
Run `npm run type-check` after all tasks. No `any` escapes and no untyped channel payloads are allowed.
</verification>

<success_criteria>
- Shared contracts/channels fully represent D-01..D-16 Phase 4 interface requirements.
- Downstream plans can implement services and UI without redefining payload shapes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-diagnostics-security-storage-and-macos-distribution-baseline/04-01-SUMMARY.md`
</output>

