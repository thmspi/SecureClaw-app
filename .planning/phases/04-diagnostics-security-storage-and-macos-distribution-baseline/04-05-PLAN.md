---
phase: 04-diagnostics-security-storage-and-macos-distribution-baseline
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - electron-builder.yml
  - build/entitlements.mac.plist
  - scripts/release/notarize.mjs
  - scripts/release/preflight-macos-signing.sh
  - scripts/release/verify-macos-artifact.sh
autonomous: true
requirements: [SEC-03]
user_setup:
  - service: apple-developer
    why: "Required for Developer ID signing identity and notarization submission credentials."
    env_vars:
      - name: APPLE_KEYCHAIN_PROFILE
        source: "Local keychain profile configured via `xcrun notarytool store-credentials`"
    dashboard_config:
      - task: "Create or confirm Developer ID Application certificate is installed in login keychain."
        location: "Apple Developer account + macOS Keychain Access"
must_haves:
  truths:
    - "macOS build artifacts are configured to produce signed `.dmg` and `.zip` outputs."
    - "Build pipeline includes notarization + stapling hooks."
    - "Verification gate runs codesign verify, Gatekeeper assessment, stapler validation, and launch smoke test."
  artifacts:
    - path: "electron-builder.yml"
      provides: "mac target configuration for dmg/zip, hardened runtime, entitlements, notarization hook."
    - path: "build/entitlements.mac.plist"
      provides: "Hardened runtime entitlements for app and inherited binaries."
    - path: "scripts/release/notarize.mjs"
      provides: "Automated notarization + stapling invocation for mac build output."
    - path: "scripts/release/verify-macos-artifact.sh"
      provides: "Automated release-gate verification commands."
  key_links:
    - from: "electron-builder.yml"
      to: "scripts/release/notarize.mjs"
      via: "afterSign hook."
      pattern: "afterSign: scripts/release/notarize.mjs"
    - from: "electron-builder.yml"
      to: "build/entitlements.mac.plist"
      via: "mac entitlements + entitlementsInherit."
      pattern: "entitlements"
---

<objective>
Create the enterprise macOS distribution baseline for signed/notarized SecureClaw artifacts with automated verification gates.

Purpose: Satisfy SEC-03 and D-17..D-20 with deterministic build + verification scripts.
Output: builder config, entitlements, notarization hook, and verification scripts.
</objective>

<execution_context>
@/Users/thomas/.codex/get-shit-done/workflows/execute-plan.md
@/Users/thomas/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-diagnostics-security-storage-and-macos-distribution-baseline/04-CONTEXT.md
@.planning/phases/04-diagnostics-security-storage-and-macos-distribution-baseline/04-RESEARCH.md
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add electron-builder mac distribution config for signed dmg+zip</name>
  <read_first>
    package.json
    .planning/phases/04-diagnostics-security-storage-and-macos-distribution-baseline/04-CONTEXT.md
    .planning/phases/04-diagnostics-security-storage-and-macos-distribution-baseline/04-RESEARCH.md
  </read_first>
  <files>electron-builder.yml, build/entitlements.mac.plist</files>
  <behavior>
    - Test 1: builder config includes `mac.target` values `dmg` and `zip`.
    - Test 2: config enables hardened runtime and points both entitlements keys to the same plist.
  </behavior>
  <action>
    Create `electron-builder.yml` with exact mac config values:
    `target: [dmg, zip]` (D-17),
    `hardenedRuntime: true` (D-18),
    `entitlements: build/entitlements.mac.plist`,
    `entitlementsInherit: build/entitlements.mac.plist`,
    `afterSign: scripts/release/notarize.mjs` (D-19).
    Create `build/entitlements.mac.plist` with required hardened-runtime permissions used by Electron app binaries.
  </action>
  <verify>
    <automated>test -f electron-builder.yml && test -f build/entitlements.mac.plist</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "target:\\s*\\[dmg, zip\\]" electron-builder.yml` returns one match.
    - `rg -n "hardenedRuntime:\\s*true" electron-builder.yml` returns one match.
    - `rg -n "afterSign:\\s*scripts/release/notarize.mjs" electron-builder.yml` returns one match.
    - `rg -n "<key>com.apple.security.cs" build/entitlements.mac.plist` returns at least one match.
  </acceptance_criteria>
  <done>Builder config and entitlements enforce signed dmg/zip with hardened runtime defaults.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement notarization/stapling hook with explicit credential preflight</name>
  <read_first>
    electron-builder.yml
    .planning/phases/04-diagnostics-security-storage-and-macos-distribution-baseline/04-RESEARCH.md
  </read_first>
  <files>scripts/release/notarize.mjs, scripts/release/preflight-macos-signing.sh</files>
  <behavior>
    - Test 1: preflight exits non-zero when `APPLE_KEYCHAIN_PROFILE` is missing.
    - Test 2: notarize hook calls `xcrun notarytool` and `xcrun stapler staple` for app path output.
  </behavior>
  <action>
    Create `scripts/release/preflight-macos-signing.sh` that checks:
    `security find-identity -v -p codesigning` has a `Developer ID Application` identity and
    `APPLE_KEYCHAIN_PROFILE` is non-empty.
    Fail fast with plain-language remediation steps if either check fails.

    Create `scripts/release/notarize.mjs` to:
    read `context.appOutDir` and packaged `.app` path,
    run `xcrun notarytool submit --keychain-profile "$APPLE_KEYCHAIN_PROFILE" --wait`,
    then run `xcrun stapler staple <appPath>`.
    Print deterministic log lines for start/success/failure.
  </action>
  <verify>
    <automated>node --check scripts/release/notarize.mjs && bash -n scripts/release/preflight-macos-signing.sh</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "APPLE_KEYCHAIN_PROFILE" scripts/release/preflight-macos-signing.sh` returns at least one match.
    - `rg -n "Developer ID Application" scripts/release/preflight-macos-signing.sh` returns one match.
    - `rg -n "notarytool submit" scripts/release/notarize.mjs` returns one match.
    - `rg -n "stapler staple" scripts/release/notarize.mjs` returns one match.
  </acceptance_criteria>
  <done>Notarization hook and credential preflight are scripted and syntax-valid.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add automated release-gate verification script for D-20 checks</name>
  <read_first>
    scripts/release/notarize.mjs
    .planning/phases/04-diagnostics-security-storage-and-macos-distribution-baseline/04-CONTEXT.md
    .planning/phases/04-diagnostics-security-storage-and-macos-distribution-baseline/04-RESEARCH.md
  </read_first>
  <files>scripts/release/verify-macos-artifact.sh</files>
  <behavior>
    - Test 1: script runs `codesign --verify`, `spctl --assess`, and `xcrun stapler validate`.
    - Test 2: script performs launch smoke test on app binary and fails on non-zero startup.
  </behavior>
  <action>
    Create `scripts/release/verify-macos-artifact.sh` that accepts one required argument: `.app` path.
    Run these checks in order:
    `codesign --verify --deep --strict --verbose=2 "$APP_PATH"`,
    `spctl --assess --type execute --verbose "$APP_PATH"`,
    `xcrun stapler validate "$APP_PATH"`,
    launch smoke test via `"$APP_PATH/Contents/MacOS/SecureClaw"` with 10-second timeout and non-zero failure on crash.
    Print PASS/FAIL marker per check and exit non-zero if any step fails (D-20).
  </action>
  <verify>
    <automated>bash -n scripts/release/verify-macos-artifact.sh</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "codesign --verify --deep --strict --verbose=2" scripts/release/verify-macos-artifact.sh` returns one match.
    - `rg -n "spctl --assess --type execute --verbose" scripts/release/verify-macos-artifact.sh` returns one match.
    - `rg -n "xcrun stapler validate" scripts/release/verify-macos-artifact.sh` returns one match.
    - `rg -n "Contents/MacOS/SecureClaw" scripts/release/verify-macos-artifact.sh` returns one match.
  </acceptance_criteria>
  <done>Release verification gate script enforces all D-20 baseline checks.</done>
</task>

</tasks>

<verification>
Run `node --check scripts/release/notarize.mjs`, `bash -n scripts/release/preflight-macos-signing.sh`, and `bash -n scripts/release/verify-macos-artifact.sh`.
</verification>

<success_criteria>
- Build pipeline is configured for signed dmg+zip outputs with hardened runtime and entitlements.
- Notarization and stapling automation is in place with explicit credential preflight.
- Distribution verification gate covers codesign, Gatekeeper, stapler, and launch smoke test.
</success_criteria>

<output>
After completion, create `.planning/phases/04-diagnostics-security-storage-and-macos-distribution-baseline/04-05-SUMMARY.md`
</output>

