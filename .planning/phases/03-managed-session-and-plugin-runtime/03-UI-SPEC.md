---
phase: 3
slug: managed-session-and-plugin-runtime
status: approved
shadcn_initialized: true
preset: b3cVZaBjQw
created: 2026-04-02
reviewed_at: 2026-04-02
---

# Phase 3 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn |
| Preset | b3cVZaBjQw (radix-luma style, stone base) |
| Component library | Radix UI |
| Icon library | Lucide React |
| Font (body) | Figtree Variable |
| Font (headings) | Geist Variable |

*Inherited from Phase 2 — no changes.*

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing, badge padding |
| md | 16px | Default element spacing, list item padding |
| lg | 24px | Section padding, card internal padding |
| xl | 32px | Layout gaps between sections |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level top/bottom spacing |

Exceptions: 44px for touch targets (action buttons in toolbar)

*Inherited from Phase 2 — no changes.*

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 16px | 400 | 1.5 |
| Label | 14px | 500 | 1.4 |
| Heading | 24px | 600 | 1.2 |
| Display | 32px | 600 | 1.1 |

*Inherited from Phase 2 — no changes.*

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `--background` (oklch 1 0 0) | Page background, surfaces |
| Secondary (30%) | `--card` / `--muted` | Section cards, history rows, plugin list |
| Accent (10%) | `--primary` (oklch 0.553 0.195 38.402) | Start Session CTA, active session indicator, success icons ONLY |
| Destructive | `--destructive` (oklch 0.577 0.245 27.325) | Stop Session button, failed status badges only |

Accent reserved for: **Start Session CTA button, active session status indicator, successful completion icons** — never for secondary links, borders, or decorative elements.

*Inherited from Phase 2 — accent expanded for session indicators.*

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Page heading | "Session Management" |
| Session section heading | "Session Control" |
| Plugin section heading | "Plugin Runs" |
| History section heading | "Operation History" |
| Start session CTA | "Start Session" |
| Stop session CTA | "Stop Session" |
| Session status: inactive | "No active session" |
| Session status: starting | "Starting session..." |
| Session status: active | "Session active" |
| Session status: stopping | "Stopping session..." |
| Plugin disabled reason | "Start a session to run plugins" |
| Run plugin CTA | "Run" |
| Retry plugin CTA | "Retry" |
| Empty state (plugins) heading | "No plugin runs yet" |
| Empty state (plugins) body | "Start a session, then run a plugin to see execution here." |
| Empty state (history) heading | "No operations recorded" |
| Empty state (history) body | "Session and plugin operations will appear here." |
| Error state (session) | "Session failed to start — Check that Docker is running and try again." |
| Error state (plugin) | "{Plugin} failed — Expand details for more info, then retry." |
| Destructive confirmation (stop) | "Stop Session: This will terminate the active session and any running plugins. Continue?" |

---

## Component Inventory

Based on CONTEXT.md decisions (D-01 through D-18), this phase requires:

### Page Layout Components
- **Card** — Section containers for Session Control, Plugin Runs, Operation History
- **Button** — Start/Stop session, Run/Retry plugin actions
- **Separator** — Visual division between page sections

### Session Control Components
- **Badge** — Session status indicator (inactive/starting/active/stopping)
- **Button** — Primary Start Session CTA, destructive Stop Session
- **Skeleton** — Loading state during session transition

### Plugin Runs Components
- **Table** — Plugin list with name, status, actions columns
- **Badge** — Plugin lifecycle state (Queued, Starting, Running, Completed, Failed)
- **Button** — Run action (disabled when no session), Retry action (for failed)
- **Collapsible** — Expandable log/progress detail per plugin
- **Progress** — Running plugin progress indicator
- **Tooltip** — Disabled state reason on hover

### Operation History Components
- **Table** — History rows with timestamp, operation type, status, duration
- **Collapsible** — Expandable detail panel per operation
- **Select** — Filter by operation type
- **Select** — Filter by status
- **Input** (type date) — Filter by date range
- **Badge** — Operation type (session/plugin) and status visual distinction

### Feedback Components
- **Alert** — Error display with expandable technical details
- **Dialog** — Stop Session confirmation modal

---

## Interaction Contracts

### Page Layout (from D-11, D-12, D-14)
- Single page with three stacked sections: Session Control (top), Plugin Runs (middle), Operation History (bottom)
- Top-right action cluster contains Start/Stop Session only (no other actions)
- Page accessible when prerequisites satisfied (post-install landing)
- Sections use Card components with lg (24px) internal padding

### Session Control (from D-01 through D-06, D-13)
- Session does NOT auto-start on page load
- Start Session CTA prominent (primary variant) when no active session
- Session status badge: `inactive` (muted) → `starting` (muted + spinner) → `active` (primary/green) → `stopping` (muted + spinner)
- Session becomes "active" only after readiness confirmation (not just process spawn)
- Stop button: one-click, shows confirmation dialog, then "stopping" state
- Disabled plugin actions show explicit reason text: "Start a session to run plugins"

### Plugin Runs (from D-07 through D-10)
- Plugin list displayed in table format with columns: Name, Status, Actions
- Run button disabled with tooltip when no active session
- Plugin lifecycle states displayed via Badge:
  - `Queued` — muted/gray
  - `Starting` — muted + spinner
  - `Running` — primary/accent + progress indicator
  - `Completed` — success/green checkmark
  - `Failed` — destructive/red
- Click row or chevron to expand log/progress detail (Collapsible)
- Retry button appears for Failed plugins (restarts from scratch, preserves history)
- Multiple plugins can run in parallel (capped concurrency, agent discretion on limit)

### Operation History (from D-15 through D-18)
- History persists session start/stop and plugin execution operations
- Summary rows show: timestamp, operation type, status, duration
- Click row to expand details (Collapsible)
- Filter controls inline above table:
  - Operation type: All | Session | Plugin
  - Status: All | Completed | Failed | Running
  - Date: From/To date pickers
- Visual distinction: Session operations use `--muted` badge, Plugin operations use `--secondary` badge
- Retention: last 90 days (backend responsibility)

---

## State Machines

### Session State Machine

```
[Inactive] --Start--> [Starting] --Ready--> [Active]
                           |                    |
                         Fail               Stop
                           |                    |
                           v                    v
                       [Inactive] <--Done-- [Stopping]
```

States: `inactive`, `starting`, `active`, `stopping`

### Plugin Execution State Machine

```
[Idle] --Run--> [Queued] --Slot Available--> [Starting] --Ready--> [Running]
                                                 |                      |
                                               Fail                 Complete/Fail
                                                 |                      |
                                                 v                      v
                                             [Failed]              [Completed/Failed]
                                                 |
                                              Retry
                                                 |
                                                 v
                                             [Queued]
```

States: `idle`, `queued`, `starting`, `running`, `completed`, `failed`

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Card, Button, Badge, Table, Collapsible, Progress, Select, Input, Alert, Dialog, Skeleton, Tooltip, Separator | not required |

No third-party registries declared.

---

## New Components Required

Components not in existing `src/renderer/components/ui/`:

| Component | Source | Notes |
|-----------|--------|-------|
| Table | shadcn official | For plugin list and operation history |
| Select | shadcn official | For filter dropdowns |
| Input | shadcn official | For date range filter |
| Tooltip | shadcn official | For disabled state explanations |
| Separator | shadcn official | For section visual division |

Install command: `npx shadcn add table select input tooltip separator`

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS
- [x] Dimension 2 Visuals: PASS
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: FLAG (3 weights — matches approved Phase 2 precedent)
- [x] Dimension 5 Spacing: PASS
- [x] Dimension 6 Registry Safety: PASS

**Approval:** approved 2026-04-02

---

## Pre-Populated Sources

| Source | Decisions Used |
|--------|---------------|
| 03-CONTEXT.md | 18 (D-01 through D-18) |
| 02-UI-SPEC.md | 6 (design system, spacing, typography, color, fonts, preset) |
| components.json | yes (preset, style, existing components) |
| User input | 0 (all derived from upstream) |

---

*Phase: 03-managed-session-and-plugin-runtime*
*UI-SPEC created: 2026-04-02*
