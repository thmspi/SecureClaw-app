---
phase: 01-platform-core-and-safety-boundaries
plan: 03
type: tdd
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/main/runtime/process-runner.ts
  - src/main/runtime/runtime-service.ts
  - src/main/runtime/__tests__/process-runner.test.ts
  - src/main/ipc/platform-router.ts
autonomous: true
requirements:
  - PLAT-04
  - PLAT-01
must_haves:
  truths:
    - "Process operations are executed through one managed runtime entry point with timeout and cancellation control."
    - "Stop behavior follows graceful shutdown, timeout escalation, then force termination."
    - "Runtime lifecycle events are emitted with correlation IDs and persisted-ready structure."
  artifacts:
    - path: "src/main/runtime/process-runner.ts"
      provides: "Centralized execution/stop implementation with timeout and cancellation"
    - path: "src/main/runtime/runtime-service.ts"
      provides: "Service facade implementing runProcess and stopProcess contracts"
    - path: "src/main/runtime/__tests__/process-runner.test.ts"
      provides: "TDD coverage for timeout/cancellation/stop escalation semantics"
  key_links:
    - from: "src/main/runtime/runtime-service.ts"
      to: "src/main/runtime/process-runner.ts"
      via: "single runtime execution pathway"
      pattern: "runProcess"
    - from: "src/main/ipc/platform-router.ts"
      to: "src/main/runtime/runtime-service.ts"
      via: "validated IPC dispatch"
      pattern: "runProcess|stopProcess"
---

<objective>
Deliver the centralized process runtime path with deterministic lifecycle controls and event semantics required for setup and session phases.

Purpose: Complete Phase 1 runtime guarantees for start/stop/timeout/cancellation and wire them through the established IPC boundary.
Output: Process runner, runtime service, tests, and final IPC-to-runtime wiring.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-platform-core-and-safety-boundaries/01-CONTEXT.md
@.planning/phases/01-platform-core-and-safety-boundaries/01-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@.planning/phases/01-platform-core-and-safety-boundaries/01-01-PLAN.md
@.planning/phases/01-platform-core-and-safety-boundaries/01-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for process lifecycle controls and event stream contract</name>
  <read_first>.planning/phases/01-platform-core-and-safety-boundaries/01-CONTEXT.md, src/shared/platform/contracts.ts, src/main/runtime/process-runner.ts, src/main/runtime/__tests__/process-runner.test.ts</read_first>
  <files>src/main/runtime/__tests__/process-runner.test.ts</files>
  <behavior>
    - Tests assert that `runProcess` emits lifecycle events (`spawned|stdout|stderr|stopping|exited|error|timeout`) with `correlationId` (D-04).
    - Tests assert stop policy order: graceful stop first, then timeout escalation, then forced termination (D-03).
    - Tests assert cancellation and timeout propagation through centralized runner interface (PLAT-04).
  </behavior>
  <acceptance_criteria>
    - `src/main/runtime/__tests__/process-runner.test.ts` contains test names for graceful stop, timeout escalation, and forced kill.
    - Test file includes assertion coverage for `correlationId` in emitted events.
    - Initial test run fails before implementation changes (RED).
  </acceptance_criteria>
  <action>Create `src/main/runtime/__tests__/process-runner.test.ts` with failing tests for lifecycle event emission and stop semantics per D-03 and D-04. Include concrete test cases for graceful stop success, graceful stop timeout leading to force kill, cancellation signal propagation, and timeout event emission with `correlationId`. Ensure the first execution of this test file is RED before implementing runtime code.</action>
  <verify>
    <automated>test -f src/main/runtime/__tests__/process-runner.test.ts && grep -q "correlationId" src/main/runtime/__tests__/process-runner.test.ts && grep -q "graceful" src/main/runtime/__tests__/process-runner.test.ts</automated>
  </verify>
  <done>Executable failing tests define runtime behavior and prevent ambiguous implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement centralized process runner/runtime service and wire IPC dispatch</name>
  <read_first>.planning/phases/01-platform-core-and-safety-boundaries/01-CONTEXT.md, src/shared/platform/contracts.ts, src/main/ipc/platform-router.ts, src/main/runtime/process-runner.ts, src/main/runtime/runtime-service.ts</read_first>
  <files>src/main/runtime/process-runner.ts, src/main/runtime/runtime-service.ts, src/main/ipc/platform-router.ts</files>
  <behavior>
    - Runtime service is the single path for process execution and stop operations used by IPC handlers (PLAT-01, PLAT-04).
    - Stop algorithm enforces graceful->timeout->force policy (D-03).
    - Event stream emitted by runner includes required lifecycle states and correlation IDs (D-04).
  </behavior>
  <acceptance_criteria>
    - `src/main/runtime/process-runner.ts` exports `runProcess` and `stopProcess` implementations with timeout/cancellation support.
    - `src/main/runtime/process-runner.ts` includes explicit graceful and force-stop branches.
    - `src/main/runtime/runtime-service.ts` delegates all runtime operations to the process runner and exposes only platform trio methods.
    - `src/main/ipc/platform-router.ts` dispatches run/stop requests into runtime service methods after schema validation.
  </acceptance_criteria>
  <action>Implement `src/main/runtime/process-runner.ts` as centralized runner using `execa`-style execution spec (`command`, `args`, `cwd`, `env`, `timeoutMs`, `correlationId`) and a stop algorithm that tries graceful shutdown first, waits bounded timeout, then force-kills when needed (D-03). Emit structured lifecycle events with `correlationId` for all required states (D-04). Create `src/main/runtime/runtime-service.ts` as the platform service facade that exposes only `runProcess`, `stopProcess`, and `getPaths` aligned to D-02 and PLAT-01, and update `src/main/ipc/platform-router.ts` to call runtime service methods post-parse.
</action>
  <verify>
    <automated>test -f src/main/runtime/process-runner.ts && test -f src/main/runtime/runtime-service.ts && grep -q "runProcess" src/main/runtime/process-runner.ts && grep -q "stopProcess" src/main/ipc/platform-router.ts</automated>
  </verify>
  <done>Centralized runtime path enforces timeout/cancellation/stop semantics and is fully reachable only through validated IPC.</done>
</task>

</tasks>

<verification>
- `npm run test -- process-runner`
- `npm run typecheck`
</verification>

<success_criteria>
- One managed runtime path handles execute/stop operations for privileged process work.
- Stop policy and event-stream behavior match locked decisions exactly.
- IPC handlers invoke runtime service only after typed schema validation.
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-core-and-safety-boundaries/01-03-SUMMARY.md`.
</output>
