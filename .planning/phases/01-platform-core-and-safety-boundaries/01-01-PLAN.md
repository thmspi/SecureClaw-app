---
phase: 01-platform-core-and-safety-boundaries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/platform/contracts.ts
  - src/shared/ipc/platform-channels.ts
  - src/main/ipc/platform-router.ts
  - src/preload/platform-api.ts
  - src/preload/index.ts
autonomous: true
requirements:
  - PLAT-01
  - SEC-02
must_haves:
  truths:
    - "Renderer can only request privileged platform operations via typed preload APIs."
    - "Main process accepts platform requests only on versioned typed channels with schema validation."
  artifacts:
    - path: "src/shared/platform/contracts.ts"
      provides: "Authoritative contracts for runProcess/stopProcess/getPaths and shared event/result types"
    - path: "src/shared/ipc/platform-channels.ts"
      provides: "Versioned IPC channel constants and zod request/response schemas"
    - path: "src/main/ipc/platform-router.ts"
      provides: "Typed IPC handlers that validate payloads before dispatch"
    - path: "src/preload/platform-api.ts"
      provides: "Narrow preload bridge exposing only approved platform methods"
  key_links:
    - from: "src/preload/platform-api.ts"
      to: "src/shared/ipc/platform-channels.ts"
      via: "invoke channel constants"
      pattern: "PLATFORM_CHANNELS"
    - from: "src/main/ipc/platform-router.ts"
      to: "src/shared/ipc/platform-channels.ts"
      via: "runtime schema parse before handler execution"
      pattern: "parse\("
---

<objective>
Create the Phase 1 contract and IPC safety foundation so all privileged runtime work is mediated through one typed and validated boundary.

Purpose: Establish immutable safety boundaries before runtime/path/binary implementations.
Output: Shared contracts, versioned IPC channel definitions, validated main-process platform router, and narrow preload API.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-platform-core-and-safety-boundaries/01-CONTEXT.md
@.planning/phases/01-platform-core-and-safety-boundaries/01-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define shared platform contracts and versioned typed IPC schemas</name>
  <read_first>.planning/phases/01-platform-core-and-safety-boundaries/01-CONTEXT.md, .planning/REQUIREMENTS.md, src/shared/platform/contracts.ts, src/shared/ipc/platform-channels.ts</read_first>
  <files>src/shared/platform/contracts.ts, src/shared/ipc/platform-channels.ts</files>
  <behavior>
    - Contract exports exactly `runProcess`, `stopProcess`, and `getPaths` request/response/event shapes per D-01 and D-02.
    - Runtime event union includes `spawned`, `stdout`, `stderr`, `stopping`, `exited`, `error`, and `timeout` keyed by `correlationId` per D-04.
    - IPC channel names are versioned (for example, `platform:v1:*`) and include zod schemas for request validation per D-07.
  </behavior>
  <acceptance_criteria>
    - `src/shared/platform/contracts.ts` exports `RunProcessRequest`, `StopProcessRequest`, and `GetPathsRequest` types.
    - `src/shared/platform/contracts.ts` contains all lifecycle event literals: `spawned|stdout|stderr|stopping|exited|error|timeout`.
    - `src/shared/ipc/platform-channels.ts` contains `platform:v1:runProcess`, `platform:v1:stopProcess`, and `platform:v1:getPaths`.
    - `src/shared/ipc/platform-channels.ts` imports `zod` and defines schema objects for each IPC request.
  </acceptance_criteria>
  <action>Implement canonical shared contracts in `src/shared/platform/contracts.ts` as the only source of truth (D-01). Define concrete types for `runProcess`, `stopProcess`, and `getPaths` with extensible option objects (D-02), include `correlationId` on all runtime events (D-04), and define typed IPC channel constants plus zod schemas in `src/shared/ipc/platform-channels.ts` with explicit `platform:v1:*` names and runtime parse support (D-07).</action>
  <verify>
    <automated>test -f src/shared/platform/contracts.ts && test -f src/shared/ipc/platform-channels.ts && grep -q "platform:v1:runProcess" src/shared/ipc/platform-channels.ts && grep -q "spawned" src/shared/platform/contracts.ts</automated>
  </verify>
  <done>Shared platform and IPC contracts are explicit, versioned, and schema-validated with no ambiguous shapes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce privileged access through preload and validated main IPC router</name>
  <read_first>.planning/phases/01-platform-core-and-safety-boundaries/01-CONTEXT.md, src/shared/ipc/platform-channels.ts, src/main/ipc/platform-router.ts, src/preload/platform-api.ts, src/preload/index.ts</read_first>
  <files>src/main/ipc/platform-router.ts, src/preload/platform-api.ts, src/preload/index.ts</files>
  <behavior>
    - Main process handlers parse request payloads through shared zod schemas before any privileged operation dispatch (D-07).
    - Preload exposes only narrow typed methods that map to the platform channels; renderer does not receive raw `ipcRenderer` privileged surface (D-08).
  </behavior>
  <acceptance_criteria>
    - `src/main/ipc/platform-router.ts` registers handlers for all three platform channels and calls schema parse before invoking service methods.
    - `src/preload/platform-api.ts` exports only `runProcess`, `stopProcess`, and `getPaths` APIs.
    - `src/preload/index.ts` uses `contextBridge.exposeInMainWorld` for the platform API and does not expose raw `ipcRenderer`.
  </acceptance_criteria>
  <action>Create `src/main/ipc/platform-router.ts` to register `ipcMain.handle` for `platform:v1:runProcess`, `platform:v1:stopProcess`, and `platform:v1:getPaths`, validating each payload using shared zod schemas before delegation (D-07). Implement `src/preload/platform-api.ts` as the renderer-facing wrapper that invokes only those channels, and wire it in `src/preload/index.ts` via `contextBridge.exposeInMainWorld('secureClaw', { platform: ... })` without exposing raw privileged primitives (D-08).</action>
  <verify>
    <automated>test -f src/main/ipc/platform-router.ts && test -f src/preload/platform-api.ts && grep -q "contextBridge.exposeInMainWorld" src/preload/index.ts && ! grep -q "exposeInMainWorld.*ipcRenderer" src/preload/index.ts</automated>
  </verify>
  <done>Renderer privileged operations are constrained to a typed, validated IPC route with no direct privileged channel leakage.</done>
</task>

</tasks>

<verification>
Run TypeScript compile and focused contract checks after implementation:
- `npm run typecheck`
- `npm test -- platform`
</verification>

<success_criteria>
- All platform privileged requests flow through versioned typed IPC channels with runtime validation.
- Shared contract module is the only source of truth for platform request/response/event shapes.
- Preload boundary exposes only the sanctioned trio and hides privileged internals from renderer.
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-core-and-safety-boundaries/01-01-SUMMARY.md`.
</output>
