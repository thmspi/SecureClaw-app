---
phase: 01-platform-core-and-safety-boundaries
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/main/platform/path-service.ts
  - src/main/platform/binary-resolver.ts
  - src/main/platform/index.ts
  - src/main/platform/__tests__/binary-resolver.test.ts
autonomous: true
requirements:
  - PLAT-02
  - PLAT-03
must_haves:
  truths:
    - "App data/work/log path computation is derived from Electron path APIs rather than hardcoded OS strings."
    - "Binary resolution uses one shared precedence order with platform naming support and health diagnostics."
  artifacts:
    - path: "src/main/platform/path-service.ts"
      provides: "Centralized platform path accessors based on Electron app APIs"
    - path: "src/main/platform/binary-resolver.ts"
      provides: "Single resolver for OpenClaw/NemoClaw binaries with precedence and health result"
    - path: "src/main/platform/index.ts"
      provides: "Export surface for path service and resolver contracts"
  key_links:
    - from: "src/main/platform/binary-resolver.ts"
      to: "src/main/platform/path-service.ts"
      via: "managed cache path fallback"
      pattern: "getPaths"
    - from: "src/main/platform/index.ts"
      to: "src/main/platform/binary-resolver.ts"
      via: "centralized exports"
      pattern: "export"
---

<objective>
Implement platform path and binary resolution services so path/binary behavior is deterministic, centralized, and Windows-ready.

Purpose: Remove hardcoded path logic and scattered binary lookup before process runner implementation.
Output: Path service using Electron app APIs and shared binary resolver with precedence and health diagnostics.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-platform-core-and-safety-boundaries/01-CONTEXT.md
@.planning/phases/01-platform-core-and-safety-boundaries/01-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build centralized path service using Electron app path APIs</name>
  <read_first>.planning/phases/01-platform-core-and-safety-boundaries/01-CONTEXT.md, src/shared/platform/contracts.ts, src/main/platform/path-service.ts</read_first>
  <files>src/main/platform/path-service.ts, src/main/platform/index.ts</files>
  <behavior>
    - Path APIs resolve appData/logs/work/cache directories from Electron app APIs only (D-02).
    - No hardcoded OS absolute path prefixes are embedded in service logic (PLAT-02).
  </behavior>
  <acceptance_criteria>
    - `src/main/platform/path-service.ts` imports Electron app APIs and Node `path`.
    - `src/main/platform/path-service.ts` contains helper functions for app data, logs, and managed cache/work directories.
    - No string literals `/Users/`, `/Applications/`, or `C:\\` appear in `src/main/platform/path-service.ts`.
    - `src/main/platform/index.ts` exports path service functions used by other services.
  </acceptance_criteria>
  <action>Implement `src/main/platform/path-service.ts` with concrete functions `getAppDataDir`, `getLogsDir`, `getWorkDir(projectId)`, and `getManagedCacheDir` derived from `app.getPath('userData')` and `app.getPath('logs')` plus `path.join` (PLAT-02). Export these functions through `src/main/platform/index.ts` to keep one import surface for downstream services.</action>
  <verify>
    <automated>test -f src/main/platform/path-service.ts && grep -q "app.getPath('userData')" src/main/platform/path-service.ts && ! grep -q "/Users/\|/Applications/\|C:\\\\" src/main/platform/path-service.ts</automated>
  </verify>
  <done>All platform-owned data path derivation is centralized and Electron API-driven without hardcoded OS roots.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement binary resolver precedence and health diagnostics</name>
  <read_first>.planning/phases/01-platform-core-and-safety-boundaries/01-CONTEXT.md, src/shared/platform/contracts.ts, src/main/platform/path-service.ts, src/main/platform/binary-resolver.ts, src/main/platform/__tests__/binary-resolver.test.ts</read_first>
  <files>src/main/platform/binary-resolver.ts, src/main/platform/__tests__/binary-resolver.test.ts, src/main/platform/index.ts</files>
  <behavior>
    - Resolver checks binary locations in this exact order: configured path, bundled binaries, system PATH, managed cache (D-05).
    - Resolver supports platform-specific suffix handling (for example `.exe` on Windows) behind one shared interface (PLAT-03).
    - Resolver health result includes resolved path, executable check result, version, failure reason, and remediation hint (D-06).
  </behavior>
  <acceptance_criteria>
    - `src/main/platform/binary-resolver.ts` contains the precedence order comment or logic for configured->bundled->PATH->cache.
    - `src/main/platform/binary-resolver.ts` includes platform suffix handling logic that appends `.exe` when needed.
    - `src/main/platform/binary-resolver.ts` exports a health result object with keys `resolvedPath`, `isExecutable`, `version`, `failureReason`, and `remediationHint`.
    - `src/main/platform/__tests__/binary-resolver.test.ts` has tests for precedence and `.exe` handling.
  </acceptance_criteria>
  <action>Implement `src/main/platform/binary-resolver.ts` with deterministic lookup order exactly as D-05 (`configuredPath` -> `bundledDir` -> `PATH` -> `managedCache`) and add platform naming normalization for Windows suffixes (`.exe`). Return structured health diagnostics per D-06 with concrete fields: `resolvedPath`, `version`, `isExecutable`, `failureReason`, `remediationHint`. Add tests in `src/main/platform/__tests__/binary-resolver.test.ts` for precedence ordering and suffix handling, and export resolver APIs from `src/main/platform/index.ts`.</action>
  <verify>
    <automated>test -f src/main/platform/binary-resolver.ts && test -f src/main/platform/__tests__/binary-resolver.test.ts && grep -q "\.exe" src/main/platform/binary-resolver.ts && grep -q "configured" src/main/platform/binary-resolver.ts</automated>
  </verify>
  <done>Binary path and health checks are deterministic, platform-aware, and consumed through one resolver service.</done>
</task>

</tasks>

<verification>
- `npm run test -- binary-resolver`
- `npm run typecheck`
</verification>

<success_criteria>
- Path derivation uses Electron APIs only and is centralized.
- One resolver service handles all platform-specific binary naming and lookup precedence.
- Resolver diagnostics are structured for downstream UI and support tooling.
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-core-and-safety-boundaries/01-02-SUMMARY.md`.
</output>
